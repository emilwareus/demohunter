---
phase: 06-oss-readiness-and-agent-skill
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - skills/demohunter/SKILL.md
  - skills/demohunter/references/authoring.md
  - skills/demohunter/references/cli.md
  - skills/demohunter/references/troubleshooting.md
  - skills/demohunter/assets/tour.template.ts
  - tests/skills/demohunter-skill-contract.test.ts
autonomous: true
requirements:
  - OSS-02
user_setup: []
must_haves:
  truths:
    - "The repo ships one installable markdown skill bundle that teaches agents how to create or update valid `.tour.ts` files without inventing product-side abstractions."
    - "The skill instructions stay aligned with the current SDK and CLI surface through automated contract checks."
    - "The shipped template and referenced paths are valid inside this repo and useful for Codex or Claude users."
  artifacts:
    - path: skills/demohunter/SKILL.md
      provides: "Canonical installable skill entrypoint"
      contains: "tour creation and update workflow"
    - path: skills/demohunter/assets/tour.template.ts
      provides: "Valid starter tour template for agents to adapt"
      contains: "defineTour"
    - path: tests/skills/demohunter-skill-contract.test.ts
      provides: "Static contract checks for the skill bundle"
      contains: "path and template validation"
---

<objective>
Package the companion agent skill for DemoHunter as plain markdown-installable instructions plus a valid template and contract checks.

Purpose: satisfy OSS-02 with one canonical skill bundle that Codex or Claude users can actually install and trust.
Output: `skills/demohunter/` with `SKILL.md`, focused references, one valid template asset, and automated checks against repo drift.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
@docs/phase_1_oss_core.md
@packages/sdk/src/tour.ts
@packages/sdk/src/runtime-types.ts
@packages/cli/src/commands/generate.ts
@/Users/emilwareus/.codex/skills/.system/skill-creator/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the installable DemoHunter skill bundle</name>
  <files>skills/demohunter/SKILL.md, skills/demohunter/references/authoring.md, skills/demohunter/references/cli.md, skills/demohunter/references/troubleshooting.md, skills/demohunter/assets/tour.template.ts</files>
  <read_first>
    - .planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
    - docs/phase_1_oss_core.md
    - packages/sdk/src/tour.ts
    - packages/sdk/src/runtime-types.ts
    - /Users/emilwareus/.codex/skills/.system/skill-creator/SKILL.md
  </read_first>
  <behavior>
    - `SKILL.md` stays concise and points to reference files instead of carrying all details inline.
    - The instructions keep DemoHunter aligned with normal Playwright usage and explicitly avoid auth/bootstrap abstractions.
    - The template asset is valid against the current SDK contract and easy for an agent to adapt.
  </behavior>
  <action>Create `skills/demohunter/` as the canonical companion skill. Write `SKILL.md` with activation guidance, workflow steps for inspecting config and existing tours, and clear rules about preserving normal Playwright code. Put detailed guidance in `references/authoring.md`, `references/cli.md`, and `references/troubleshooting.md`. Add `assets/tour.template.ts` as a valid minimal template that uses the real `defineTour`/`chapter`/`step`/`narrate` surface and demonstrates the expected structure without drifting into a fake abstraction layer.</action>
  <verify>
    <automated>test -f skills/demohunter/SKILL.md && test -f skills/demohunter/references/authoring.md && test -f skills/demohunter/references/cli.md && test -f skills/demohunter/references/troubleshooting.md && test -f skills/demohunter/assets/tour.template.ts</automated>
    <automated>rg -n "defineTour|chapter|step|narrate" skills/demohunter/assets/tour.template.ts && rg -n "references/authoring.md|references/cli.md|assets/tour.template.ts" skills/demohunter/SKILL.md</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `skills/demohunter/SKILL.md` exists and references the shipped `references/` and `assets/` files.
    - The template tour is a valid TypeScript DemoHunter tour shape, not pseudocode.
    - The instructions explicitly tell agents to preserve normal Playwright patterns and repo-local verification.
  </acceptance_criteria>
  <done>The repo now ships a real, installable DemoHunter skill bundle for agent-assisted tour authoring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add contract checks so the skill bundle cannot silently drift</name>
  <files>tests/skills/demohunter-skill-contract.test.ts</files>
  <read_first>
    - skills/demohunter/SKILL.md
    - skills/demohunter/assets/tour.template.ts
    - packages/sdk/src/tour.ts
    - packages/cli/src/commands/generate.ts
  </read_first>
  <behavior>
    - The test verifies required files exist and referenced paths resolve.
    - The test proves the template can be loaded or typechecked against the current SDK contract.
    - The test fails if the skill references stale helper names, wrong CLI commands, or missing assets.
  </behavior>
  <action>Add `tests/skills/demohunter-skill-contract.test.ts` as a static guardrail for the skill bundle. Parse or inspect `SKILL.md` enough to assert the required reference and asset paths exist, and add a template validation step that imports or typechecks the template against the current workspace code. Keep the test deterministic and structural; do not try to simulate an LLM run. The goal is to detect drift in paths, APIs, and shipped instructions.</action>
  <verify>
    <automated>bun test tests/skills/demohunter-skill-contract.test.ts</automated>
    <automated>bun run test</automated>
  </verify>
  <acceptance_criteria>
    - The skill contract test fails on missing referenced files or stale template imports.
    - The skill contract test runs in normal CI without external APIs.
    - The skill bundle is now protected against obvious doc/code drift.
  </acceptance_criteria>
  <done>OSS-02 is backed by automated contract checks instead of static markdown only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill docs -> agent behavior | The shipped markdown directly influences how agents edit user code |
| template asset -> generated tours | The template becomes the basis for future authored scripts |
| repo drift -> skill docs | SDK/CLI changes can silently invalidate the skill unless checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T | `skills/demohunter/assets/tour.template.ts` | mitigate | Validate the template against the current SDK contract in tests. |
| T-06-02-02 | R | `skills/demohunter/SKILL.md` | mitigate | Add contract checks for referenced files and current CLI command names. |
| T-06-02-03 | E | skill instructions | mitigate | Explicitly forbid invented auth/bootstrap abstractions and keep Playwright-native guidance. |
</threat_model>

<verification>
- `bun test tests/skills/demohunter-skill-contract.test.ts`
- `bun x tsc -b tsconfig.json --pretty false`
</verification>

<success_criteria>
The repo ships one installable DemoHunter skill bundle that is useful to Codex or Claude users and guarded against drift by automated contract checks.
</success_criteria>

<output>
After completion, create `.planning/phases/06-oss-readiness-and-agent-skill/06-oss-readiness-and-agent-skill-02-SUMMARY.md`
</output>
