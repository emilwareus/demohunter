---
phase: 06-oss-readiness-and-agent-skill
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - .github/workflows/ci.yml
  - LICENSE
  - README.md
  - tests/e2e/oss-onboarding-contract.test.ts
autonomous: true
requirements:
  - OSS-03
user_setup: []
must_haves:
  truths:
    - "The public repo has deterministic CI that proves the default OSS path on every push and pull request."
    - "The repo ships with a permissive license, removing legal ambiguity for OSS adoption."
    - "CI does not require a live OpenAI key by default; live narration remains optional and explicitly gated."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Default public CI workflow"
      contains: "Bun install, Playwright browser install, and repo verification"
    - path: LICENSE
      provides: "Permissive OSS license"
      contains: "MIT License"
---

<objective>
Finalize public launch readiness by adding deterministic CI and a permissive license, and by locking the public verification path to the real default OSS workflow.

Purpose: satisfy the CI and license portion of OSS-03.
Output: public CI workflow, committed license, and final verification coverage for OSS adoption readiness.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
@README.md
@package.json
@Makefile
@tests/integration/openai-narration-live.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public CI that proves the deterministic OSS path</name>
  <files>.github/workflows/ci.yml, tests/e2e/oss-onboarding-contract.test.ts</files>
  <read_first>
    - package.json
    - Makefile
    - tests/integration/openai-narration-live.test.ts
    - .planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
  </read_first>
  <behavior>
    - CI installs Bun dependencies, installs Chromium for Playwright, and runs the repo verification path without requiring `.env`.
    - CI explicitly provisions `ffmpeg` or asserts it before verification so media-generation commands are deterministic on a fresh runner.
    - Live OpenAI coverage remains optional and skipped by default unless CI injects the required secrets and opt-in flags.
    - The workflow stays simple and contributor-friendly rather than introducing multiple speculative release jobs.
  </behavior>
  <action>Create `.github/workflows/ci.yml` for push and pull request validation. The workflow should install Bun, restore/cache dependencies as appropriate, explicitly provision `ffmpeg` on the runner, install the Playwright Chromium runtime, and run the deterministic verification command for this repo. Do not require `OPENAI_API_KEY` in the default workflow; rely on the existing live integration gating so public contributors are not blocked. Add or extend a static test if needed to protect the assumptions the docs make about the CI verification command.</action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && rg -n "ffmpeg|apt-get install -y ffmpeg|brew install ffmpeg" .github/workflows/ci.yml</automated>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and targets the default repo verification path.
    - The workflow explicitly provisions or verifies `ffmpeg` before running repo verification.
    - The workflow installs the Playwright browser runtime required by e2e generation tests.
    - The default CI path does not depend on a live OpenAI key.
  </acceptance_criteria>
  <done>The repo now has a realistic public CI contract for contributors and maintainers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the permissive license and finalize public-repo readiness messaging</name>
  <files>LICENSE, README.md</files>
  <read_first>
    - README.md
    - .planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
  </read_first>
  <behavior>
    - The license choice is explicit and committed in-repo.
    - README reflects the final public posture after examples, skill docs, docs, and CI all exist.
    - The public repo no longer looks structurally unfinished.
  </behavior>
  <action>Add an MIT `LICENSE` file and update README badges or public metadata as needed so the repo clearly presents itself as an installable permissively licensed OSS project. Keep the messaging aligned with the actual implemented state after Plans 01-03 rather than aspirational future features.</action>
  <verify>
    <automated>test -f LICENSE && rg -n \"MIT License|Permission is hereby granted\" LICENSE</automated>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists and is MIT.
    - README reflects the final public OSS readiness state.
    - No public-facing file still implies the repo is missing basic OSS governance pieces.
  </acceptance_criteria>
  <done>The repo is legally and operationally ready for public OSS use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions environment -> repo verification | CI must reproduce the default deterministic verification path |
| public repo -> adopters | License and workflow presence affect legal and operational trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | D | public CI | mitigate | Install Playwright Chromium explicitly and run the existing deterministic verify command. |
| T-06-04-02 | R | license state | mitigate | Commit an explicit MIT license so adopters are not left inferring rights from repo metadata. |
| T-06-04-03 | D | live narration in CI | mitigate | Keep live OpenAI coverage gated and skipped by default to avoid contributor-facing secret dependencies. |
</threat_model>

<verification>
- `bun run verify`
- `test -f LICENSE && rg -n "MIT License|Permission is hereby granted" LICENSE`
</verification>

<success_criteria>
The repo now has deterministic public CI, an explicit permissive license, and a final OSS-ready public surface that does not depend on maintainer tribal knowledge.
</success_criteria>

<output>
After completion, create `.planning/phases/06-oss-readiness-and-agent-skill/06-oss-readiness-and-agent-skill-04-SUMMARY.md`
</output>
