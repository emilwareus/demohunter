---
phase: 06-oss-readiness-and-agent-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - examples/nextjs-demo/package.json
  - examples/nextjs-demo/next.config.ts
  - examples/nextjs-demo/app/page.tsx
  - examples/nextjs-demo/demohunter.config.ts
  - examples/nextjs-demo/demos/nextjs-demo.tour.ts
  - examples/vite-demo/package.json
  - examples/vite-demo/index.html
  - examples/vite-demo/src/main.ts
  - examples/vite-demo/src/App.tsx
  - examples/vite-demo/demohunter.config.ts
  - examples/vite-demo/demos/vite-demo.tour.ts
  - tests/e2e/examples-contract.test.ts
autonomous: true
requirements:
  - OSS-01
user_setup: []
must_haves:
  truths:
    - "The repo contains two real example apps, one Next.js and one Vite, that exercise the public DemoHunter workflow instead of internal test fixtures."
    - "Each example can be started locally and used as a generation target without publishing workspace packages first."
    - "Automated verification proves each example can generate `.demohunter/<tour-id>/` output with the current CLI/runtime contract."
  artifacts:
    - path: examples/nextjs-demo/demohunter.config.ts
      provides: "A real Next.js example consumer config"
      contains: "baseURL"
    - path: examples/vite-demo/demohunter.config.ts
      provides: "A real Vite example consumer config"
      contains: "baseURL"
    - path: tests/e2e/examples-contract.test.ts
      provides: "Consumer-path verification for both example apps"
      contains: "Next.js and Vite generation coverage"
---

<objective>
Create the Phase 6 example projects and verify them as real consumer paths so the public repo proves the intended workflow in both Next.js and Vite.

Purpose: satisfy OSS-01 with low-maintenance, representative examples rather than docs-only sample files.
Output: two thin example apps, one real tour per app, and an automated contract test that generates output against both examples.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
@docs/phase_1_oss_core.md
@package.json
@packages/create-demohunter/templates/starter/demohunter.config.ts
@tests/e2e/init-generate-smoke.test.ts
@tests/e2e/built-cli-bin-contract.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add thin workspace examples for Next.js and Vite</name>
  <files>package.json, bun.lock, examples/nextjs-demo/package.json, examples/nextjs-demo/next.config.ts, examples/nextjs-demo/app/page.tsx, examples/nextjs-demo/demohunter.config.ts, examples/nextjs-demo/demos/nextjs-demo.tour.ts, examples/vite-demo/package.json, examples/vite-demo/index.html, examples/vite-demo/src/main.ts, examples/vite-demo/src/App.tsx, examples/vite-demo/demohunter.config.ts, examples/vite-demo/demos/vite-demo.tour.ts</files>
  <read_first>
    - package.json
    - .planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
    - docs/phase_1_oss_core.md
    - packages/create-demohunter/templates/starter/demohunter.config.ts
  </read_first>
  <behavior>
    - Each example app is intentionally small, deterministic, and local-only.
    - Each example includes one valid `.tour.ts` file that uses the real SDK contract and targets the example UI with stable selectors.
    - The root workspace recognizes `examples/*` so a fresh repo install resolves example dependencies without extra manual linking.
  </behavior>
  <action>Extend the root workspace to include `examples/*`, then create `examples/nextjs-demo` and `examples/vite-demo` as minimal consumer apps. Keep both examples small: one page, obvious labels, one or two state transitions, no external network dependencies, and selectors meant for demo generation rather than framework showcase depth. Add a `demohunter.config.ts` and one `.tour.ts` per example using the real SDK/runtime shape. For the Next.js example, configure local workspace consumption cleanly, including `transpilePackages` if needed to consume source packages without publish-time build assumptions.</action>
  <verify>
    <automated>bun --cwd examples/nextjs-demo x next build</automated>
    <automated>bun --cwd examples/vite-demo x vite build</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` workspace config includes `examples/*`.
    - `examples/nextjs-demo` and `examples/vite-demo` each contain a runnable app, `demohunter.config.ts`, and one `.tour.ts`.
    - Neither example depends on external APIs or unpublished package artifacts.
    - Each example has a framework-native build path that passes before end-to-end generation coverage is added.
  </acceptance_criteria>
  <done>Two real consumer examples exist and stay aligned with the actual DemoHunter product surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove both example apps generate DemoHunter output end to end</name>
  <files>tests/e2e/examples-contract.test.ts</files>
  <read_first>
    - tests/e2e/init-generate-smoke.test.ts
    - tests/e2e/built-cli-bin-contract.test.ts
    - .planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
  </read_first>
  <behavior>
    - The test boots each example app on a local port, runs `demohunter generate`, and asserts portable output exists.
    - The test stays consumer-oriented and avoids fragile UI assertions beyond what is needed to prove generation succeeds.
    - The example contract test can run in CI without live OpenAI by using no-narration or already-deterministic narration strategy as appropriate.
  </behavior>
  <action>Add `tests/e2e/examples-contract.test.ts` to manage the full example verification path. Reuse the repo's existing end-to-end test style: create temp output roots, start the example dev server, wait for local readiness in the harness rather than inside product code, run the CLI from the example root, and assert `.demohunter/<tour-id>/manifest.json`, `chapters.json`, and `video.mp4` exist. Keep ports explicit and cleanup deterministic so the examples do not become flaky CI baggage.</action>
  <verify>
    <automated>bun test tests/e2e/examples-contract.test.ts</automated>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/examples-contract.test.ts` verifies both example apps, not just one.
    - The assertions check for real generated output under `.demohunter/<tour-id>/`.
    - The example verification path runs from the example roots as a consumer would.
  </acceptance_criteria>
  <done>OSS-01 is proven by real example generation rather than by static files alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace packages -> example apps | Local workspace packages are consumed as if they were public package dependencies |
| example dev servers -> e2e harness | The test harness depends on stable local app startup and teardown |
| example tours -> generator output | Example scripts become public adoption references and must stay valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | D | example dev servers | mitigate | Keep apps tiny and local-only; use explicit ports and deterministic startup/teardown in the harness. |
| T-06-01-02 | T | example tour files | mitigate | Reuse the real SDK contract and cover the example tours in automated e2e so drift is caught. |
| T-06-01-03 | R | workspace example consumption | mitigate | Wire examples into the root workspace and verify consumer-path generation without package publishing. |
</threat_model>

<verification>
- `bun test tests/e2e/examples-contract.test.ts`
- `bun x tsc -b tsconfig.json --pretty false`
</verification>

<success_criteria>
Both example apps exist as low-maintenance public consumer paths, and automated verification proves they generate real DemoHunter output with the current workspace code.
</success_criteria>

<output>
After completion, create `.planning/phases/06-oss-readiness-and-agent-skill/06-oss-readiness-and-agent-skill-01-SUMMARY.md`
</output>
