---
phase: 06-oss-readiness-and-agent-skill
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - README.md
  - docs/getting-started.md
  - docs/troubleshooting.md
  - packages/cli/src/commands/generate.ts
  - packages/cli/src/commands/init.ts
  - packages/cli/src/commands/generate.test.ts
  - tests/e2e/oss-onboarding-contract.test.ts
autonomous: true
requirements:
  - OSS-03
user_setup: []
must_haves:
  truths:
    - "A new OSS user can follow the public repo docs and understand the real adoption path without maintainer help."
    - "The CLI surfaces clearer errors at the main first-run failure points instead of generic stack noise."
    - "Docs, examples, and skill installation guidance all point at the same product boundary and verification commands."
  artifacts:
    - path: README.md
      provides: "Primary public onboarding document"
      contains: "quickstart and product boundary"
    - path: docs/getting-started.md
      provides: "Expanded setup and example walkthrough"
      contains: "prerequisites"
    - path: docs/troubleshooting.md
      provides: "Known failure modes and fixes"
      contains: "ffmpeg and browser runtime guidance"
    - path: tests/e2e/oss-onboarding-contract.test.ts
      provides: "Verification for onboarding-path errors and docs-linked commands"
      contains: "first-run guardrails"
---

<objective>
Make the public repo understandable and forgiving for first-time users through aligned onboarding docs and better CLI error handling.

Purpose: satisfy the docs and better-errors parts of OSS-03.
Output: public-facing onboarding docs, troubleshooting guidance, and improved adoption-path CLI errors with automated coverage.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
@docs/phase_1_oss_core.md
@package.json
@packages/cli/src/commands/generate.ts
@packages/cli/src/commands/generate.test.ts
@packages/cli/src/commands/init.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public onboarding docs that reflect the real OSS workflow</name>
  <files>README.md, docs/getting-started.md, docs/troubleshooting.md</files>
  <read_first>
    - .planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
    - docs/phase_1_oss_core.md
    - package.json
  </read_first>
  <behavior>
    - README is the main public entry point and stays concise.
    - Deeper setup and failure-mode detail lives in a small docs set, not scattered prose.
    - The docs clearly state what DemoHunter does not do: app startup, auth/session abstraction, or test-runner orchestration.
  </behavior>
  <action>Create a root `README.md` with product summary, prerequisites, install/quickstart, examples, skill-install note, and verification commands. Add `docs/getting-started.md` for a fuller setup and first-generation walkthrough. Add `docs/troubleshooting.md` for the most likely adoption blockers: missing Playwright browser runtime, missing `ffmpeg`, missing `OPENAI_API_KEY` for uncached narration, and confusion about base URL/app readiness responsibilities. Keep the docs centered on the actual repo structure and Phase 6 examples.</action>
  <verify>
    <automated>test -f README.md && test -f docs/getting-started.md && test -f docs/troubleshooting.md</automated>
    <automated>rg -n "ffmpeg|OPENAI_API_KEY|Playwright|examples|skills/demohunter|demohunter generate" README.md docs/getting-started.md docs/troubleshooting.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists and contains prerequisites, quickstart, examples, and verification commands.
    - The docs explicitly communicate the Playwright-native product boundary.
    - Troubleshooting covers the main first-run failure points rather than generic debugging advice.
  </acceptance_criteria>
  <done>The public repo now has a coherent onboarding surface for new OSS users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden adoption-path CLI errors and verify them</name>
  <files>packages/cli/src/commands/generate.ts, packages/cli/src/commands/init.ts, packages/cli/src/commands/generate.test.ts, tests/e2e/oss-onboarding-contract.test.ts</files>
  <read_first>
    - packages/cli/src/commands/generate.ts
    - packages/cli/src/commands/generate.test.ts
    - packages/cli/src/commands/init.ts
    - .planning/phases/06-oss-readiness-and-agent-skill/06-RESEARCH.md
  </read_first>
  <behavior>
    - Error improvements focus on adoption blockers, not every internal edge case in the repo.
    - Error text is actionable and points the user at the correct next step or docs section.
    - Tests cover the new message surfaces so future refactors do not regress them.
  </behavior>
  <action>Improve CLI error handling around the first-run path. Focus on the clearest user-facing blockers: missing browser runtime, missing `ffmpeg`, missing uncached-narration OpenAI credentials, invalid tour entrypoint shape, and obvious app/baseURL reachability failures under the product's Playwright-native boundary. Add or update unit/e2e tests so these errors stay specific and actionable, and ensure docs-linked wording does not drift silently.</action>
  <verify>
    <automated>bun test packages/cli/src/commands/generate.test.ts</automated>
    <automated>bun test tests/e2e/oss-onboarding-contract.test.ts</automated>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - The improved errors name the failing prerequisite or boundary clearly.
    - Tests cover the new messages or error categories.
    - The docs and CLI point at the same adoption flow instead of contradicting each other.
  </acceptance_criteria>
  <done>OSS-03 onboarding is materially better because new users get clear docs and clearer failure guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI internals -> public error text | Internal failures are translated into user-facing guidance |
| README/docs -> user expectations | Documentation shapes how users interpret product boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | R | onboarding docs | mitigate | Align README, deeper docs, and example commands around one real adoption path. |
| T-06-03-02 | I | CLI error text | mitigate | Improve only the high-value prerequisite and boundary failures and cover them with tests. |
| T-06-03-03 | E | product expectations | mitigate | State clearly that DemoHunter does not manage app startup, auth, or `@playwright/test` flows. |
</threat_model>

<verification>
- `bun test packages/cli/src/commands/generate.test.ts`
- `bun test tests/e2e/oss-onboarding-contract.test.ts`
- `bun run verify`
</verification>

<success_criteria>
New OSS users can follow the public repo docs, encounter clearer errors when prerequisites are missing, and understand the actual DemoHunter boundary without maintainer intervention.
</success_criteria>

<output>
After completion, create `.planning/phases/06-oss-readiness-and-agent-skill/06-oss-readiness-and-agent-skill-03-SUMMARY.md`
</output>
