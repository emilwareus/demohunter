---
phase: 03-playwright-generation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/generator-playwright/src/execute/replay-timeline.ts
  - packages/generator-playwright/src/execute/replay-timeline.test.ts
  - packages/generator-playwright/src/record/screencast.ts
  - packages/generator-playwright/src/record/screencast.test.ts
autonomous: true
requirements:
  - GEN-02
  - GEN-03
user_setup: []
must_haves:
  truths:
    - "The recorded pass replays the pass-1 timeline instead of free-running the tour a second time."
    - "Every narrated action pauses for `durationMs + holdPaddingMs` during pass 2."
    - "Recorded-pass divergence fails clearly instead of emitting a best-effort artifact."
  artifacts:
    - path: packages/generator-playwright/src/execute/replay-timeline.ts
      provides: "Strict pass-2 replay engine"
      contains: "replayTimeline"
    - path: packages/generator-playwright/src/record/screencast.ts
      provides: "Playwright screencast session wrapper"
      contains: "startScreencast"
  key_links:
    - from: packages/generator-playwright/src/execute/replay-timeline.ts
      to: packages/generator-playwright/src/execute/generator-types.ts
      via: "CollectedTimeline consumption"
      pattern: "CollectedTimeline"
    - from: packages/generator-playwright/src/record/screencast.ts
      to: packages/generator-playwright/src/execute/replay-timeline.ts
      via: "recording session around strict replay"
      pattern: "startScreencast|replayTimeline"
---

<objective>
Build the strict recorded-pass engine and the Playwright screencast wrapper that will power video capture.

Purpose: turn the pass-1 timeline into a deterministic, clearly failing pass-2 replay instead of a second uncontrolled tour run.
Output: strict replay logic plus screencast start/stop helpers ready for final generator wiring.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-playwright-generation-engine/03-CONTEXT.md
@.planning/phases/03-playwright-generation-engine/03-RESEARCH.md
@.planning/phases/03-playwright-generation-engine/03-01-PLAN.md
@packages/generator-playwright/src/execute/generator-types.ts
@packages/generator-playwright/src/execute/collect-timeline.ts
@packages/generator-playwright/src/runtime/create-smoke-tour-runtime.ts

<interfaces>
Plan 01 creates the pass-1 replay contract. Plan 02 consumes it directly:

```typescript
export type CollectedTimeline = {
  tourId: string;
  baseURL: string;
  entries: CollectedTimelineEntry[];
};

export type CollectedTimelineEntry =
  | { kind: "chapter"; title: string; chapterId?: string; index: number }
  | { kind: "step-start" | "step-end"; title: string; chapterTitle?: string; index: number }
  | { kind: "narrate"; text: string; durationMs: number; chapterTitle?: string; index: number };
```

Pass 2 must consume this structure, run the tour again through the same runtime helper surface, and fail as soon as the live event stream diverges from the expected entry sequence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict pass-2 replay from the collected timeline</name>
  <files>packages/generator-playwright/src/execute/replay-timeline.ts, packages/generator-playwright/src/execute/replay-timeline.test.ts</files>
  <read_first>
    - packages/generator-playwright/src/execute/replay-timeline.ts (new file target)
    - packages/generator-playwright/src/execute/replay-timeline.test.ts (new file target)
    - packages/generator-playwright/src/execute/generator-types.ts
    - packages/generator-playwright/src/execute/collect-timeline.ts
    - packages/generator-playwright/src/runtime/create-smoke-tour-runtime.ts
    - .planning/phases/03-playwright-generation-engine/03-CONTEXT.md
    - docs/phase_1_oss_core.md
  </read_first>
  <behavior>
    - Test 1: `replayTimeline()` executes the same lifecycle order as pass 1 and compares each emitted runtime event against the expected collected entry sequence.
    - Test 2: On each narration event, the replay engine waits exactly `event.durationMs + config.holdPaddingMs`.
    - Test 3: When event order, chapter, step title, or narration text diverges, replay throws a dedicated clear error with the expected entry index and context instead of continuing.
  </behavior>
  <action>Create `packages/generator-playwright/src/execute/replay-timeline.ts` with an exported `replayTimeline()` function that accepts the loaded config, validated tour, Playwright `Page`, and a `CollectedTimeline`. Re-run the tour through the shared runtime helper surface, capture live events, and compare them entry-by-entry against the expected timeline from pass 1. For narration events, call a passed-in wait dependency or `page.waitForTimeout()` with `durationMs + loadedConfig.config.holdPaddingMs`. Per D-08 and D-09, divergence must fail immediately with an explicit generator error that names the mismatched event and index; do not continue recording, suppress the error, or downgrade to warnings. Keep authors on the Playwright-native runtime surface rather than inventing a separate replay DSL.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/execute/replay-timeline.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `replayTimeline()` consumes `CollectedTimeline` directly and compares pass-2 runtime events to pass-1 expectations in order.
    - Narration waits use `durationMs + holdPaddingMs` exactly once per narrate event.
    - Divergence throws a clear generator-specific error without producing a best-effort success path.
    - The tests cover matching replay, hold-padding timing, and at least one divergent step/narration failure.
  </acceptance_criteria>
  <done>Pass 2 is a strict replay engine that enforces determinism instead of simply running the tour twice and hoping the output matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap Playwright screencast control for recorded-pass use</name>
  <files>packages/generator-playwright/src/record/screencast.ts, packages/generator-playwright/src/record/screencast.test.ts</files>
  <read_first>
    - packages/generator-playwright/src/record/screencast.ts (new file target)
    - packages/generator-playwright/src/record/screencast.test.ts (new file target)
    - packages/generator-playwright/src/execute/replay-timeline.ts
    - .planning/phases/03-playwright-generation-engine/03-RESEARCH.md
    - docs/phase_1_oss_core.md
  </read_first>
  <behavior>
    - Test 1: `startScreencast()` calls `page.screencast.start()` with the requested temp output path and viewport dimensions.
    - Test 2: When `showActions` is true, the wrapper calls `page.screencast.showActions()` with brief annotation settings suitable for the later minimal-overlay wiring.
    - Test 3: `stopScreencast()` preserves the primary replay failure by only surfacing stop/cleanup errors when no earlier replay error exists.
  </behavior>
  <action>Create `packages/generator-playwright/src/record/screencast.ts` with exported helpers for `startScreencast()` and `stopScreencast()` around Playwright's `page.screencast` API. The start helper must accept a temp output path, viewport size, and a `showActions` flag, then call `page.screencast.start()` and optionally `page.screencast.showActions()` using brief annotation defaults that stay consistent with D-04 and D-05. The stop helper must stop the screencast and preserve any earlier replay failure, following the primary-error-over-cleanup pattern already used in `smoke-generate.ts`. Do not add chapter-overlay rendering in this task; this wrapper is only the recording primitive for later wiring.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/record/screencast.test.ts</automated>
    <automated>bun test packages/generator-playwright/src/execute/replay-timeline.test.ts packages/generator-playwright/src/record/screencast.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - The screencast wrapper starts and stops Playwright recording with an explicit temp file path.
    - Action annotations are opt-in and brief, not always-on or visually dominant.
    - Cleanup/stop errors cannot overwrite a previously detected replay failure.
    - The helpers do not introduce any `recordVideo` fallback or separate recorder abstraction.
  </acceptance_criteria>
  <done>The generator has a tested Playwright-native recording primitive ready to wrap the strict replay engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pass-1 timeline -> pass-2 replay | Recorded output depends entirely on trusted reuse of the collected entry sequence |
| authored tour -> live replay events | User-authored Playwright code can diverge between passes and must be detected |
| replay engine -> screencast session | Recording start/stop must not hide the primary replay outcome |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T | pass-2 replay ordering | mitigate | Compare every live runtime event against the expected collected entry sequence and fail immediately on mismatch per D-08 and D-09. |
| T-03-02-02 | R | narration timing | mitigate | Cover `durationMs + holdPaddingMs` waits in automated replay tests so timing behavior is explicit and regression-resistant. |
| T-03-02-03 | D | screencast shutdown | mitigate | Preserve the primary replay error and only surface stop/cleanup failures when no earlier replay failure exists. |
</threat_model>

<verification>
- `bun test packages/generator-playwright/src/execute/replay-timeline.test.ts`
- `bun test packages/generator-playwright/src/record/screencast.test.ts`
- `bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false`
</verification>

<success_criteria>
The repo contains a strict pass-2 replay engine that waits on narration timing plus hold padding, detects divergence clearly, and can be wrapped by Playwright screencast recording without losing the primary failure cause.
</success_criteria>

<output>
After completion, create `.planning/phases/03-playwright-generation-engine/03-playwright-generation-engine-02-SUMMARY.md`
</output>
