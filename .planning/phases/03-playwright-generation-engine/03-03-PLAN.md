---
phase: 03-playwright-generation-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/sdk/src/config.ts
  - packages/sdk/src/index.ts
  - packages/sdk/src/config.test.ts
  - packages/cli/src/config/load-config.ts
  - packages/cli/src/config/load-config.test.ts
  - packages/generator-playwright/src/record/mux-video.ts
  - packages/generator-playwright/src/record/mux-video.test.ts
  - packages/generator-playwright/src/output/prepare-output-dir.ts
  - packages/generator-playwright/src/output/prepare-output-dir.test.ts
  - packages/generator-playwright/src/output/write-generation-output.ts
  - packages/generator-playwright/src/output/write-generation-output.test.ts
autonomous: true
requirements:
  - GEN-04
  - GEN-05
user_setup: []
must_haves:
  truths:
    - "Successful generation writes artifacts under `.demohunter/<tour-id>/` relative to the current working directory."
    - "The default final artifact is `video.mp4`, while `video.webm` is only produced when explicitly selected."
    - "Phase 3 output stays baseline-scoped and does not finalize later manifest, subtitle, or poster responsibilities."
  artifacts:
    - path: packages/sdk/src/config.ts
      provides: "Record format configuration contract"
      contains: "format"
    - path: packages/generator-playwright/src/record/mux-video.ts
      provides: "Format-specific final video emission"
      contains: "muxVideo"
    - path: packages/generator-playwright/src/output/write-generation-output.ts
      provides: "Baseline `.demohunter/<tour-id>/` artifact writer"
      contains: "writeGenerationOutput"
  key_links:
    - from: packages/cli/src/config/load-config.ts
      to: packages/sdk/src/config.ts
      via: "record.format resolution"
      pattern: "DEFAULT_RECORD_CONFIG|format"
    - from: packages/generator-playwright/src/output/write-generation-output.ts
      to: packages/generator-playwright/src/record/mux-video.ts
      via: "final video placement"
      pattern: "muxVideo|video.mp4|video.webm"
---

<objective>
Add the output and media-format subsystem so the real generator can emit baseline local artifacts into the correct `.demohunter/` layout.

Purpose: separate Phase 3’s artifact-writing and format-selection responsibilities from replay logic while preserving the later Phase 4/5 boundaries.
Output: config support for `mp4`/`webm`, a muxing helper, and baseline output-directory writers.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-playwright-generation-engine/03-CONTEXT.md
@.planning/phases/03-playwright-generation-engine/03-RESEARCH.md
@.planning/phases/01-repository-and-scaffolding/01-04-SUMMARY.md
@packages/sdk/src/config.ts
@packages/cli/src/config/load-config.ts
@packages/generator-playwright/src/smoke-generate.ts

<interfaces>
Current config shape lacks output format selection:

```typescript
export type RecordConfig = {
  showActions: boolean;
  showChapters: boolean;
};
```

Phase 3 extends this to keep `mp4` default and `webm` opt-in, then uses the resolved config in output writing:

```typescript
record: {
  showActions: boolean;
  showChapters: boolean;
  format: "mp4" | "webm";
}
```

The output root contract for Phase 3 is fixed and not user-configurable:

```typescript
const outputDir = path.join(process.cwd(), ".demohunter", tourId);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config and media-format handling for `mp4` default / `webm` opt-in</name>
  <files>packages/sdk/src/config.ts, packages/sdk/src/index.ts, packages/sdk/src/config.test.ts, packages/cli/src/config/load-config.ts, packages/cli/src/config/load-config.test.ts, packages/generator-playwright/src/record/mux-video.ts, packages/generator-playwright/src/record/mux-video.test.ts</files>
  <read_first>
    - packages/sdk/src/config.ts
    - packages/sdk/src/index.ts
    - packages/sdk/src/config.test.ts
    - packages/cli/src/config/load-config.ts
    - packages/cli/src/config/load-config.test.ts
    - packages/generator-playwright/src/record/mux-video.ts (new file target)
    - packages/generator-playwright/src/record/mux-video.test.ts (new file target)
    - .planning/phases/03-playwright-generation-engine/03-CONTEXT.md
    - docs/phase_1_oss_core.md
  </read_first>
  <behavior>
    - Test 1: `DEFAULT_RECORD_CONFIG` resolves `format: "mp4"` and `loadConfig()` preserves that default when the user omits `record.format`.
    - Test 2: When the user sets `record.format: "webm"`, the resolved config returns `"webm"` without generating `mp4` by default.
    - Test 3: `muxVideo()` produces exactly one final video artifact based on the resolved format and exposes a dependency-injected ffmpeg command path for mp4 conversion.
  </behavior>
  <action>Update `packages/sdk/src/config.ts` and `packages/sdk/src/index.ts` so `RecordConfig` includes `format: "mp4" | "webm"` with a default of `"mp4"` per D-06 and D-07. Extend the SDK and CLI config tests to cover defaulting and explicit `webm` selection. Create `packages/generator-playwright/src/record/mux-video.ts` with an exported `muxVideo()` helper that accepts a temporary screencast path plus the resolved record format. For `mp4`, the helper must invoke an injected ffmpeg command and write `video.mp4`; for `webm`, it must emit `video.webm` without also producing `video.mp4`. Keep the helper implementation phase-scoped: it owns format conversion only, not poster generation, subtitles, manifest data, or audio copying.</action>
  <verify>
    <automated>bun test packages/sdk/src/config.test.ts packages/cli/src/config/load-config.test.ts packages/generator-playwright/src/record/mux-video.test.ts</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `RecordConfig` includes a typed `format` field with default `"mp4"`.
    - `loadConfig()` merges and returns `record.format` correctly for default and explicit `webm` cases.
    - `muxVideo()` can be tested without a real ffmpeg binary by dependency injection, and its tests prove `mp4` default vs `webm` opt-in behavior.
    - No config or media helper introduces subtitles, manifest schema, poster generation, or TTS-specific behavior.
  </acceptance_criteria>
  <done>The config and media subsystem now support Phase 3’s required `mp4` default and `webm` opt-in output behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement baseline `.demohunter/<tour-id>/` output writing</name>
  <files>packages/generator-playwright/src/output/prepare-output-dir.ts, packages/generator-playwright/src/output/prepare-output-dir.test.ts, packages/generator-playwright/src/output/write-generation-output.ts, packages/generator-playwright/src/output/write-generation-output.test.ts</files>
  <read_first>
    - packages/generator-playwright/src/output/prepare-output-dir.ts (new file target)
    - packages/generator-playwright/src/output/prepare-output-dir.test.ts (new file target)
    - packages/generator-playwright/src/output/write-generation-output.ts (new file target)
    - packages/generator-playwright/src/output/write-generation-output.test.ts (new file target)
    - packages/generator-playwright/src/smoke-generate.ts
    - packages/generator-playwright/src/execute/generator-types.ts
    - packages/generator-playwright/src/record/mux-video.ts
    - .planning/phases/03-playwright-generation-engine/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: The output-preparation helper refuses unsafe tour IDs and resolves the final directory to `${process.cwd()}/.demohunter/${tourId}`.
    - Test 2: `writeGenerationOutput()` writes the selected final video file plus `chapters.json` into that directory.
    - Test 3: The output writer does not emit `manifest.json`, `captions.srt`, `captions.vtt`, `poster.jpg`, or copied audio files in this phase.
  </behavior>
  <action>Create `packages/generator-playwright/src/output/prepare-output-dir.ts` to centralize tour-id validation and output-directory creation under `path.join(process.cwd(), ".demohunter", tourId)`, reusing the safe slug rules already enforced in `smoke-generate.ts` so Phase 3 locks the GEN-04 output-root contract directly. Create `packages/generator-playwright/src/output/write-generation-output.ts` to accept that resolved output directory, chapter data derived from the collected timeline, and the final video artifact returned by `muxVideo()`. The writer must persist baseline Phase 3 artifacts only: the chosen final video (`video.mp4` or `video.webm`) and `chapters.json`. Explicitly do not write manifest, subtitles, poster, or audio outputs here, because those are owned by Phase 4 and Phase 5.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/output/prepare-output-dir.test.ts packages/generator-playwright/src/output/write-generation-output.test.ts</automated>
    <automated>bun test packages/generator-playwright/src/record/mux-video.test.ts packages/generator-playwright/src/output/prepare-output-dir.test.ts packages/generator-playwright/src/output/write-generation-output.test.ts</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - Output directories are always created under `.demohunter/<tour-id>/` rooted at the current working directory, with safe-slug enforcement.
    - The baseline output writer persists the chosen final video and `chapters.json` only.
    - The tests explicitly assert that later-phase artifacts are absent from Phase 3 output writing.
    - The output helpers are self-contained and ready to be called by final generator orchestration without overlapping replay logic.
  </acceptance_criteria>
  <done>Phase 3 has a dedicated output-writing subsystem that writes the baseline local artifact set into `.demohunter/<tour-id>/` without overcommitting later output-contract work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config -> format resolution | Untrusted config controls final artifact format and destination layout |
| temp screencast file -> final output dir | Intermediate recording output becomes user-visible artifacts under `.demohunter/` |
| tour id -> filesystem path | Tour IDs influence directory creation and must remain path-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | output directory pathing | mitigate | Reuse filesystem-safe tour-id validation and centralize output-dir preparation so artifact writes cannot drift outside `.demohunter/<tour-id>/` under the current working directory. |
| T-03-03-02 | T | format selection | mitigate | Add config and muxing tests that prove `mp4` is the default and `webm` is only produced when explicitly configured, per D-06 and D-07. |
| T-03-03-03 | R | phase-boundary creep | mitigate | Keep automated assertions that Phase 3 output writing excludes manifest, subtitles, poster, and audio files deferred to later phases. |
</threat_model>

<verification>
- `bun test packages/sdk/src/config.test.ts packages/cli/src/config/load-config.test.ts packages/generator-playwright/src/record/mux-video.test.ts`
- `bun test packages/generator-playwright/src/output/prepare-output-dir.test.ts packages/generator-playwright/src/output/write-generation-output.test.ts`
- `bun x tsc -b tsconfig.json --pretty false`
</verification>

<success_criteria>
The repo can resolve a real Phase 3 record format, convert/copy the final video accordingly, and write the baseline generation artifacts into `.demohunter/<tour-id>/` while leaving manifest, subtitles, poster, and audio responsibilities for later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/03-playwright-generation-engine/03-playwright-generation-engine-03-SUMMARY.md`
</output>
