---
phase: 05-portable-output-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - packages/generator-playwright/package.json
  - packages/generator-playwright/src/execute/generator-types.ts
  - packages/generator-playwright/src/generate.ts
  - packages/generator-playwright/src/generate.test.ts
  - packages/generator-playwright/src/record/mux-video.ts
  - packages/generator-playwright/src/record/mux-video.test.ts
  - packages/generator-playwright/src/output/capture-poster.ts
  - packages/generator-playwright/src/output/capture-poster.test.ts
  - packages/generator-playwright/src/output/export-audio.ts
  - packages/generator-playwright/src/output/export-audio.test.ts
  - packages/generator-playwright/src/output/write-generation-output.ts
  - packages/generator-playwright/src/output/write-generation-output.test.ts
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-04
user_setup: []
must_haves:
  truths:
    - "Every successful generation writes baseline `video.mp4`, `poster.jpg`, `manifest.json`, `captions.srt`, `captions.vtt`, and `chapters.json`, plus copied narration audio under `audio/` and optional `video.webm` only when configured."
    - "The manifest contains replay-safe narration timing, chapter timing, file relationships, and SHA-256 checksums without leaking repo-local or cache-local absolute paths."
    - "Poster generation is deterministic from a fixed timestamp constant with a stable clamp fallback for short videos."
  artifacts:
    - path: packages/generator-playwright/src/record/mux-video.ts
      provides: "Baseline mp4 plus optional webm video emission"
      contains: "video.mp4"
    - path: packages/generator-playwright/src/output/capture-poster.ts
      provides: "Deterministic poster extraction and video duration probing"
      contains: "PORTABLE_POSTER_TIMESTAMP_MS"
    - path: packages/generator-playwright/src/output/write-generation-output.ts
      provides: "Portable artifact writing and manifest assembly"
      contains: "manifest.json"
  key_links:
    - from: packages/generator-playwright/src/generate.ts
      to: packages/generator-playwright/src/output/write-generation-output.ts
      via: "tour metadata, replay-safe narration timing, chapters, and video outputs"
      pattern: "recordedNarrations|tourTitle|videos"
    - from: packages/generator-playwright/src/output/write-generation-output.ts
      to: packages/manifest/src/index.ts
      via: "artifact descriptor creation and strict manifest validation"
      pattern: "createPortableArtifactDescriptor|parsePortableOutputManifest"
    - from: packages/generator-playwright/src/output/export-audio.ts
      to: packages/generator-playwright/src/output/write-generation-output.ts
      via: "copied `audio/` artifacts included in manifest"
      pattern: "audio/"
---

<objective>
Upgrade the generator so Phase 5 emits the full portable artifact set and writes a replay-safe, checksum-backed `manifest.json` using the `@demohunter/manifest` contract.

Purpose: turn the existing local output directory into a portable OSS-to-Cloud handoff folder rather than a partial runtime artifact dump.
Output: baseline mp4 plus optional webm handling, deterministic poster generation, copied narration audio, replay-safe manifest metadata, and portable output writer coverage.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-portable-output-contract/05-CONTEXT.md
@.planning/phases/05-portable-output-contract/05-RESEARCH.md
@.planning/phases/05-portable-output-contract/05-VALIDATION.md
@.planning/phases/03-playwright-generation-engine/03-playwright-generation-engine-03-SUMMARY.md
@.planning/phases/04-openai-narration-and-cache/04-openai-narration-and-cache-03-SUMMARY.md
@docs/phase_1_oss_core.md
@docs/phase_2_cloud_offering.md
@packages/generator-playwright/package.json
@packages/generator-playwright/src/execute/generator-types.ts
@packages/generator-playwright/src/generate.ts
@packages/generator-playwright/src/record/mux-video.ts
@packages/generator-playwright/src/output/write-generation-output.ts

<interfaces>
Consume the Plan 01 contract exactly from `@demohunter/manifest`:

```typescript
export const PORTABLE_OUTPUT_MANIFEST_VERSION = 1;
export type PortableArtifactDescriptor = {
  path: string;
  mediaType: string;
  checksum: { algorithm: "sha256"; byteSize: number; hex: string };
};
export type PortableOutputManifest = {
  manifestVersion: 1;
  tour: { id: string; title: string };
  playback: { durationMs: number };
  artifacts: {
    videos: { mp4: PortableArtifactDescriptor; webm?: PortableArtifactDescriptor };
    poster: PortableArtifactDescriptor & { captureTimestampMs: number };
    captions: { srt: PortableArtifactDescriptor; vtt: PortableArtifactDescriptor };
    chapters: PortableArtifactDescriptor;
    audio: Array<PortableArtifactDescriptor & { cacheKey: string; durationMs: number }>;
  };
  timeline: {
    chapters: Array<{ title: string; startMs: number }>;
    narrations: Array<{
      cacheKey: string;
      text: string;
      chapterTitle?: string;
      startMs: number;
      endMs: number;
      durationMs: number;
    }>;
  };
};
export function createPortableArtifactDescriptor(input: {
  outputDir: string;
  filePath: string;
  mediaType: string;
}): Promise<PortableArtifactDescriptor>;
export function parsePortableOutputManifest(value: unknown): PortableOutputManifest;
```

Extend the generator with these exact handoff types:

```typescript
type PortableVideoOutputs = {
  mp4: { fileName: "video.mp4"; format: "mp4"; path: string };
  webm?: { fileName: "video.webm"; format: "webm"; path: string };
};

type RecordedNarration = {
  cacheKey: string;
  text: string;
  chapterTitle?: string;
  audioPath: string;
  startMs: number;
  endMs: number;
  durationMs: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the generator to baseline-mp4 output and replay-safe narration timing</name>
  <files>packages/generator-playwright/package.json, packages/generator-playwright/src/execute/generator-types.ts, packages/generator-playwright/src/generate.ts, packages/generator-playwright/src/generate.test.ts, packages/generator-playwright/src/record/mux-video.ts, packages/generator-playwright/src/record/mux-video.test.ts</files>
  <read_first>
    - packages/generator-playwright/package.json
    - packages/generator-playwright/src/execute/generator-types.ts
    - packages/generator-playwright/src/generate.ts
    - packages/generator-playwright/src/generate.test.ts
    - packages/generator-playwright/src/record/mux-video.ts
    - packages/generator-playwright/src/record/mux-video.test.ts
    - packages/generator-playwright/src/execute/replay-timeline.ts
    - packages/manifest/src/index.ts
    - .planning/phases/05-portable-output-contract/05-CONTEXT.md
    - .planning/phases/05-portable-output-contract/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `muxVideo()` always emits `video.mp4`, and `video.webm` appears only when `record.format === "webm"`.
    - Test 2: `generateTour()` records replay-time narration `startMs` and `endMs` from pass 2 while leaving `captions.srt` / `captions.vtt` generation behavior untouched.
    - Test 3: The output writer input now carries `tourId`, `tourTitle`, `videos`, `chapters`, and ordered `recordedNarrations` for manifest assembly.
  </behavior>
  <action>Add `@demohunter/manifest` as a workspace dependency in `packages/generator-playwright/package.json`. In `packages/generator-playwright/src/record/mux-video.ts`, replace the old one-of video contract per D-05 and the orchestrator resolution: always ffmpeg the temporary screencast into `video.mp4`, and when `record.format === "webm"` also copy the temporary screencast to `video.webm`; otherwise remove any stale `video.webm` from prior runs. In `packages/generator-playwright/src/execute/generator-types.ts`, add `PortableVideoOutputs` and `RecordedNarration` types. In `packages/generator-playwright/src/generate.ts`, record replay-safe narration timing by inspecting the matched timeline entry on pass-2 `narrate` events, with `startMs = now() - recordingStartedAt` and `endMs = startMs + durationMs`. Forward `tourFile.tour.id`, `tourFile.tour.title`, `videos`, `chapters`, and `recordedNarrations` into `writeGenerationOutput()`. Per D-08 and the orchestrator subtitle resolution, do not rewrite `subtitles.ts`; keep replay-safe timing richness in manifest metadata instead of changing caption semantics.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/record/mux-video.test.ts</automated>
    <automated>bun test packages/generator-playwright/src/generate.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/generator-playwright/src/record/mux-video.ts` contains both `video.mp4` and optional `video.webm` output handling.
    - `packages/generator-playwright/src/execute/generator-types.ts` exports `PortableVideoOutputs` and `RecordedNarration`.
    - `packages/generator-playwright/src/generate.ts` contains `recordedNarrations`, `startMs`, and `endMs`, and passes `tourTitle` plus `videos` into `writeGenerationOutput`.
  </acceptance_criteria>
  <done>The generator now produces the Phase 5 baseline video contract and the replay-safe narration timing metadata needed for portability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write poster, copied narration audio, and the validated portable manifest into the output directory</name>
  <files>packages/generator-playwright/src/output/capture-poster.ts, packages/generator-playwright/src/output/capture-poster.test.ts, packages/generator-playwright/src/output/export-audio.ts, packages/generator-playwright/src/output/export-audio.test.ts, packages/generator-playwright/src/output/write-generation-output.ts, packages/generator-playwright/src/output/write-generation-output.test.ts</files>
  <read_first>
    - packages/generator-playwright/src/output/write-generation-output.ts
    - packages/generator-playwright/src/output/write-generation-output.test.ts
    - packages/generator-playwright/src/output/subtitles.ts
    - packages/generator-playwright/src/output/capture-poster.ts (new file target)
    - packages/generator-playwright/src/output/export-audio.ts (new file target)
    - packages/manifest/src/index.ts
    - .planning/phases/05-portable-output-contract/05-CONTEXT.md
    - .planning/phases/05-portable-output-contract/05-VALIDATION.md
    - docs/phase_2_cloud_offering.md
  </read_first>
  <behavior>
    - Test 1: Successful output writing now persists `manifest.json`, `poster.jpg`, `chapters.json`, `captions.srt`, `captions.vtt`, `video.mp4`, copied `audio/` files, and optional `video.webm`.
    - Test 2: `manifest.json` parses through `parsePortableOutputManifest()` and contains only output-root-relative POSIX paths, never cache or repo absolute paths.
    - Test 3: Poster extraction uses a preferred timestamp constant of `1_000` ms with a deterministic clamp fallback for short videos.
  </behavior>
  <action>Create `packages/generator-playwright/src/output/capture-poster.ts` and `packages/generator-playwright/src/output/export-audio.ts` as the Phase 5 media helpers per D-04, D-06, and D-07. Set `PORTABLE_POSTER_TIMESTAMP_MS = 1_000`. `capturePoster()` must probe the final `video.mp4`, compute `captureTimestampMs = Math.max(0, Math.min(PORTABLE_POSTER_TIMESTAMP_MS, durationMs > 0 ? durationMs - 1 : 0))`, and write `poster.jpg` with ffmpeg. `exportAudio()` must copy each unique narration `audioPath` into `audio/<basename(audioPath)>`, preserve the source extension, and avoid placeholder files when there are no narrations. Update `writeGenerationOutput()` to keep the existing `chapters.json` and caption behavior, then assemble the manifest per D-01 through D-05 and D-08 through D-10 with the exact shape from Plan 01: `manifestVersion: 1`, `tour.id`, `tour.title`, `playback.durationMs`, `artifacts.videos.mp4`, optional `artifacts.videos.webm`, `artifacts.poster.captureTimestampMs`, `artifacts.captions.srt`, `artifacts.captions.vtt`, `artifacts.chapters`, `artifacts.audio[]`, `timeline.chapters`, and `timeline.narrations`. Build every file descriptor with `createPortableArtifactDescriptor()`, validate the finished object with `parsePortableOutputManifest()`, and write `manifest.json` only after every final portable artifact exists. Do not include `baseURL`, `configPath`, cache-root paths, temp recording paths, or any other machine-local values in the manifest.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/output/capture-poster.test.ts packages/generator-playwright/src/output/export-audio.test.ts</automated>
    <automated>bun test packages/generator-playwright/src/output/write-generation-output.test.ts packages/generator-playwright/src/generate.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/generator-playwright/src/output/capture-poster.ts` exports `PORTABLE_POSTER_TIMESTAMP_MS = 1_000`.
    - `packages/generator-playwright/src/output/write-generation-output.ts` contains `manifest.json`, `poster.jpg`, `audio`, `createPortableArtifactDescriptor(`, and `parsePortableOutputManifest(`.
    - `packages/generator-playwright/src/output/write-generation-output.test.ts` expects `manifest.json`, `poster.jpg`, and copied `audio/` output alongside the baseline artifacts.
  </acceptance_criteria>
  <done>The generator writes the full portable Phase 5 artifact set and validates the manifest against the authoritative package contract before returning success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay timeline -> manifest metadata | Runtime event timing becomes portable ingest metadata |
| cache audio paths -> output `audio/` copies | Cache-local files are copied into the portable contract boundary |
| final media files -> poster/checksum helpers | FFmpeg and ffprobe output become user-visible portable artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | `packages/generator-playwright/src/generate.ts` | mitigate | Record narration `startMs` and `endMs` from pass-2 replay events, not pass-1 sequential subtitle timing, per D-08 and the Phase 5 subtitle compatibility resolution. |
| T-05-02-02 | I | `packages/generator-playwright/src/output/write-generation-output.ts` | mitigate | Build every manifest path from `createPortableArtifactDescriptor()` so cache-root, repo-root, temp, and secret-bearing values never enter `manifest.json` per D-03 and D-09. |
| T-05-02-03 | T | `packages/generator-playwright/src/record/mux-video.ts` | mitigate | Always emit `video.mp4` and only emit `video.webm` when configured so the video contract stays explicit and deterministic per D-05 and OUT-01. |
| T-05-02-04 | T | `packages/generator-playwright/src/output/capture-poster.ts` | mitigate | Use the fixed `1_000` ms poster constant plus deterministic clamping so reruns cannot drift to heuristic poster selection per D-06 and D-07. |
| T-05-02-05 | D | `packages/generator-playwright/src/output/export-audio.ts` | mitigate | Copy unique narration files once into `audio/` and reuse cache keys in the manifest so repeated narrations do not multiply portable artifacts or corrupt timing relationships. |
</threat_model>

<verification>
- `bun test packages/generator-playwright/src/record/mux-video.test.ts`
- `bun test packages/generator-playwright/src/output/capture-poster.test.ts packages/generator-playwright/src/output/export-audio.test.ts`
- `bun test packages/generator-playwright/src/output/write-generation-output.test.ts packages/generator-playwright/src/generate.test.ts`
- `bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false`
</verification>

<success_criteria>
Successful generator runs now emit the full Phase 5 portable artifact layout and a validated manifest with relative paths, SHA-256 checksums, deterministic poster metadata, and replay-safe narration timing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-portable-output-contract/05-portable-output-contract-02-SUMMARY.md`
</output>
