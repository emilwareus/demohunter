---
phase: 05-portable-output-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - packages/generator-playwright/src/execute/generator-types.ts
  - packages/generator-playwright/src/generate.ts
  - packages/generator-playwright/src/generate.test.ts
  - packages/generator-playwright/src/record/mux-video.ts
  - packages/generator-playwright/src/record/mux-video.test.ts
autonomous: true
requirements:
  - OUT-01
  - OUT-04
user_setup: []
must_haves:
  truths:
    - "Every successful generation now produces baseline `video.mp4`, with `video.webm` emitted only when the recording config explicitly requests it."
    - "Replay-safe narration timing is captured from pass-2 playback for manifest use, while `captions.srt` and `captions.vtt` behavior stays unchanged per D-08."
    - "The output writer receives stable portable handoff inputs for tour identity, video artifacts, chapters, and recorded narrations instead of reconstructing them later."
  artifacts:
    - path: packages/generator-playwright/src/record/mux-video.ts
      provides: "Baseline mp4 plus optional webm video emission"
      contains: "video.mp4"
    - path: packages/generator-playwright/src/execute/generator-types.ts
      provides: "Portable output handoff types for later manifest assembly"
      contains: "PortableVideoOutputs"
    - path: packages/generator-playwright/src/generate.ts
      provides: "Replay-safe narration timing capture and output-writer wiring"
      contains: "recordedNarrations"
  key_links:
    - from: packages/generator-playwright/src/record/mux-video.ts
      to: packages/generator-playwright/src/execute/generator-types.ts
      via: "shared `PortableVideoOutputs` return shape"
      pattern: "PortableVideoOutputs"
    - from: packages/generator-playwright/src/generate.ts
      to: packages/generator-playwright/src/output/write-generation-output.ts
      via: "forwarded `tourId`, `tourTitle`, `videos`, `chapters`, and `recordedNarrations`"
      pattern: "recordedNarrations|tourTitle|videos"
    - from: packages/generator-playwright/src/generate.ts
      to: packages/generator-playwright/src/output/subtitles.ts
      via: "subtitle generation remains unchanged while manifest timing becomes richer"
      pattern: "subtitles"
---

<objective>
Promote the generator runtime to the closed Phase 5 video contract and capture replay-safe narration timing before output assembly begins.

Purpose: separate generator/runtime plumbing from later poster/audio/manifest writing so the portable-output execution stays bounded and dependency-driven.
Output: baseline mp4 plus optional webm handling, typed portable handoff data, and replay-measured narration timing forwarded to the output writer.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-portable-output-contract/05-CONTEXT.md
@.planning/phases/05-portable-output-contract/05-RESEARCH.md
@.planning/phases/05-portable-output-contract/05-VALIDATION.md
@.planning/phases/03-playwright-generation-engine/03-playwright-generation-engine-03-SUMMARY.md
@.planning/phases/04-openai-narration-and-cache/04-openai-narration-and-cache-03-SUMMARY.md
@docs/phase_1_oss_core.md
@docs/phase_2_cloud_offering.md
@packages/generator-playwright/src/execute/generator-types.ts
@packages/generator-playwright/src/generate.ts
@packages/generator-playwright/src/record/mux-video.ts
@packages/generator-playwright/src/output/write-generation-output.ts

<interfaces>
Plan 02 defines the generator-side handoff Plan 03 will consume:

```typescript
type PortableVideoOutputs = {
  mp4: { fileName: "video.mp4"; format: "mp4"; path: string };
  webm?: { fileName: "video.webm"; format: "webm"; path: string };
};

type RecordedNarration = {
  cacheKey: string;
  text: string;
  chapterTitle?: string;
  audioPath: string;
  startMs: number;
  endMs: number;
  durationMs: number;
};
```

Plan 03 will consume these values through `writeGenerationOutput()` together with the Plan 01 manifest package exports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock `muxVideo()` to baseline mp4 plus optional webm output</name>
  <files>packages/generator-playwright/src/execute/generator-types.ts, packages/generator-playwright/src/record/mux-video.ts, packages/generator-playwright/src/record/mux-video.test.ts</files>
  <read_first>
    - packages/generator-playwright/src/execute/generator-types.ts
    - packages/generator-playwright/src/record/mux-video.ts
    - packages/generator-playwright/src/record/mux-video.test.ts
    - .planning/phases/05-portable-output-contract/05-CONTEXT.md
    - .planning/phases/05-portable-output-contract/05-RESEARCH.md
    - .planning/phases/05-portable-output-contract/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `muxVideo()` always emits `video.mp4` and returns it through `PortableVideoOutputs.mp4`.
    - Test 2: `video.webm` is emitted only when `record.format === "webm"`, and non-webm runs remove any stale `video.webm`.
    - Test 3: The generator-side video output type makes required mp4 and optional webm explicit for downstream manifest work.
  </behavior>
  <action>Create `PortableVideoOutputs` in `packages/generator-playwright/src/execute/generator-types.ts` and use it as the `muxVideo()` return contract per D-05 and the resolved research record. In `packages/generator-playwright/src/record/mux-video.ts`, always ffmpeg the temporary screencast into `video.mp4`. When `record.format === "webm"`, also copy the temporary screencast to `video.webm`; otherwise remove any stale `video.webm` from prior runs. Update `packages/generator-playwright/src/record/mux-video.test.ts` to prove the required mp4 plus optional webm behavior, including stale-webm cleanup.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/record/mux-video.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/generator-playwright/src/execute/generator-types.ts` exports `PortableVideoOutputs`.
    - `packages/generator-playwright/src/record/mux-video.ts` contains both `video.mp4` and optional `video.webm` handling.
    - `packages/generator-playwright/src/record/mux-video.test.ts` asserts stale `video.webm` cleanup for non-webm runs.
  </acceptance_criteria>
  <done>The generator video contract now matches Phase 5 exactly: baseline mp4, optional webm, no placeholder files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture replay-safe narration timing and forward typed output inputs</name>
  <files>packages/generator-playwright/src/generate.ts, packages/generator-playwright/src/generate.test.ts</files>
  <read_first>
    - packages/generator-playwright/src/generate.ts
    - packages/generator-playwright/src/generate.test.ts
    - packages/generator-playwright/src/execute/replay-timeline.ts
    - packages/generator-playwright/src/output/write-generation-output.ts
    - packages/generator-playwright/src/output/subtitles.ts
    - packages/generator-playwright/src/execute/generator-types.ts
    - .planning/phases/05-portable-output-contract/05-CONTEXT.md
    - .planning/phases/05-portable-output-contract/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `generateTour()` records replay-time narration `startMs` and `endMs` from pass-2 matched events.
    - Test 2: `writeGenerationOutput()` is called with `tourId`, `tourTitle`, `videos`, `chapters`, and ordered `recordedNarrations`.
    - Test 3: Caption generation behavior remains unchanged while replay-safe timing richness moves into manifest-bound metadata only.
  </behavior>
  <action>Add `RecordedNarration` to `packages/generator-playwright/src/execute/generator-types.ts`, then update `packages/generator-playwright/src/generate.ts` to capture replay-safe narration timing from pass-2 `narrate` events using the actual recording clock: `startMs = now() - recordingStartedAt`, `endMs = startMs + durationMs`. Forward `tourFile.tour.id`, `tourFile.tour.title`, `videos`, `chapters`, and `recordedNarrations` into `writeGenerationOutput()`. Per D-08 and the resolved subtitle decision, do not rewrite `subtitles.ts`; keep Phase 4 caption semantics intact and add the richer timing only for later manifest assembly.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/generate.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/generator-playwright/src/execute/generator-types.ts` exports `RecordedNarration`.
    - `packages/generator-playwright/src/generate.ts` contains `recordedNarrations`, `startMs`, and `endMs`.
    - `packages/generator-playwright/src/generate.ts` forwards `tourTitle` plus `videos` into `writeGenerationOutput()`.
  </acceptance_criteria>
  <done>The generator now provides replay-safe timing and typed portable-output inputs without changing subtitle behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pass-2 replay events -> recorded narration timing | Runtime playback timing becomes future portable metadata |
| recording config -> video outputs | User configuration controls whether optional webm is emitted |
| generator runtime -> output writer | Phase 5 portable contract inputs are handed off from execution to output assembly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | `packages/generator-playwright/src/record/mux-video.ts` | mitigate | Always emit `video.mp4` and only emit `video.webm` when configured so runtime behavior matches the resolved Phase 5 contract per D-05 and OUT-01. |
| T-05-02-02 | D | `packages/generator-playwright/src/record/mux-video.ts` | mitigate | Remove stale `video.webm` when the run is not configured for webm so prior artifacts cannot poison later manifest/output assertions. |
| T-05-02-03 | T | `packages/generator-playwright/src/generate.ts` | mitigate | Record narration `startMs` and `endMs` from pass-2 replay events rather than pass-1 sequential subtitle timing, preserving ingest-safe timing fidelity per D-08 and OUT-04. |
| T-05-02-04 | R | `packages/generator-playwright/src/generate.ts` | mitigate | Forward explicit `tourId`, `tourTitle`, `videos`, `chapters`, and `recordedNarrations` into the output writer so later plans do not reconstruct the portable contract from implicit runtime state. |
</threat_model>

<verification>
- `bun test packages/generator-playwright/src/record/mux-video.test.ts`
- `bun test packages/generator-playwright/src/generate.test.ts`
- `bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false`
</verification>

<success_criteria>
The generator runtime now emits the resolved video artifact contract and exposes replay-safe narration timing plus typed output inputs that later Phase 5 plans can assemble into the portable manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/05-portable-output-contract/05-portable-output-contract-02-SUMMARY.md`
</output>
