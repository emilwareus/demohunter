---
phase: 05-portable-output-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - packages/generator-playwright/package.json
  - packages/generator-playwright/src/output/capture-poster.ts
  - packages/generator-playwright/src/output/capture-poster.test.ts
  - packages/generator-playwright/src/output/export-audio.ts
  - packages/generator-playwright/src/output/export-audio.test.ts
  - packages/generator-playwright/src/output/write-generation-output.ts
  - packages/generator-playwright/src/output/write-generation-output.test.ts
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-04
user_setup: []
must_haves:
  truths:
    - "Successful output writing now adds deterministic `poster.jpg`, copied narration audio under `audio/`, and a validated `manifest.json` to the portable artifact set."
    - "The manifest contains only output-root-relative POSIX paths plus SHA-256 checksums, and it models required `video.mp4` with optional `video.webm` exactly once."
    - "Manifest assembly happens only after all final portable artifacts exist, so checksums describe exported bytes rather than cache-local or temp files."
  artifacts:
    - path: packages/generator-playwright/src/output/capture-poster.ts
      provides: "Deterministic poster extraction and video duration probing"
      contains: "PORTABLE_POSTER_TIMESTAMP_MS"
    - path: packages/generator-playwright/src/output/export-audio.ts
      provides: "Narration audio copy/export into `audio/`"
      contains: "audio/"
    - path: packages/generator-playwright/src/output/write-generation-output.ts
      provides: "Portable artifact writing and manifest assembly"
      contains: "manifest.json"
  key_links:
    - from: packages/generator-playwright/src/output/capture-poster.ts
      to: "video.mp4"
      via: "ffprobe duration plus deterministic ffmpeg frame capture"
      pattern: "PORTABLE_POSTER_TIMESTAMP_MS"
    - from: packages/generator-playwright/src/output/export-audio.ts
      to: packages/generator-playwright/src/output/write-generation-output.ts
      via: "copied `audio/` artifacts included in manifest assembly"
      pattern: "audio/"
    - from: packages/generator-playwright/src/output/write-generation-output.ts
      to: packages/manifest/src/index.ts
      via: "artifact descriptor creation and strict manifest validation"
      pattern: "createPortableArtifactDescriptor|parsePortableOutputManifest"
---

<objective>
Add the remaining output-layer pieces for the portable contract: deterministic poster capture, narration-audio export, and validated manifest writing from final artifacts.

Purpose: isolate media-helper creation and output/manifest integration from the generator runtime plumbing so execution stays within context bounds.
Output: `poster.jpg`, copied `audio/` artifacts, and `manifest.json` written from final bytes through the `@demohunter/manifest` boundary.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-portable-output-contract/05-CONTEXT.md
@.planning/phases/05-portable-output-contract/05-RESEARCH.md
@.planning/phases/05-portable-output-contract/05-VALIDATION.md
@.planning/phases/03-playwright-generation-engine/03-playwright-generation-engine-03-SUMMARY.md
@.planning/phases/04-openai-narration-and-cache/04-openai-narration-and-cache-03-SUMMARY.md
@docs/phase_1_oss_core.md
@docs/phase_2_cloud_offering.md
@packages/generator-playwright/package.json
@packages/generator-playwright/src/output/write-generation-output.ts
@packages/manifest/src/index.ts

<interfaces>
Consume the Plan 01 manifest contract and the Plan 02 generator handoff exactly:

```typescript
export type PortableOutputManifest = {
  manifestVersion: 1;
  tour: { id: string; title: string };
  playback: { durationMs: number };
  artifacts: {
    videos: { mp4: PortableArtifactDescriptor; webm?: PortableArtifactDescriptor };
    poster: PortableArtifactDescriptor & { captureTimestampMs: number };
    captions: { srt: PortableArtifactDescriptor; vtt: PortableArtifactDescriptor };
    chapters: PortableArtifactDescriptor;
    audio: Array<PortableArtifactDescriptor & { cacheKey: string; durationMs: number }>;
  };
  timeline: {
    chapters: Array<{ title: string; startMs: number }>;
    narrations: Array<{
      cacheKey: string;
      text: string;
      chapterTitle?: string;
      startMs: number;
      endMs: number;
      durationMs: number;
    }>;
  };
};

type PortableVideoOutputs = {
  mp4: { fileName: "video.mp4"; format: "mp4"; path: string };
  webm?: { fileName: "video.webm"; format: "webm"; path: string };
};

type RecordedNarration = {
  cacheKey: string;
  text: string;
  chapterTitle?: string;
  audioPath: string;
  startMs: number;
  endMs: number;
  durationMs: number;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic poster and narration-audio export helpers</name>
  <files>packages/generator-playwright/src/output/capture-poster.ts, packages/generator-playwright/src/output/capture-poster.test.ts, packages/generator-playwright/src/output/export-audio.ts, packages/generator-playwright/src/output/export-audio.test.ts</files>
  <read_first>
    - packages/generator-playwright/src/output/write-generation-output.ts
    - packages/generator-playwright/src/output/subtitles.ts
    - packages/generator-playwright/src/output/capture-poster.ts
    - packages/generator-playwright/src/output/export-audio.ts
    - .planning/phases/05-portable-output-contract/05-CONTEXT.md
    - .planning/phases/05-portable-output-contract/05-RESEARCH.md
    - .planning/phases/05-portable-output-contract/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `capturePoster()` uses a preferred timestamp constant of `1_000` ms and deterministically clamps for short videos.
    - Test 2: `exportAudio()` copies each unique narration `audioPath` into `audio/<basename>` while preserving the source extension.
    - Test 3: No placeholder `audio/` files are emitted when a run has no narrations.
  </behavior>
  <action>Create `packages/generator-playwright/src/output/capture-poster.ts` and `packages/generator-playwright/src/output/export-audio.ts` per D-04, D-06, and D-07. Set `PORTABLE_POSTER_TIMESTAMP_MS = 1_000`. `capturePoster()` must probe final `video.mp4`, compute `captureTimestampMs = Math.max(0, Math.min(PORTABLE_POSTER_TIMESTAMP_MS, durationMs > 0 ? durationMs - 1 : 0))`, and write `poster.jpg` via ffmpeg. `exportAudio()` must copy each unique narration `audioPath` into `audio/<basename(audioPath)>`, preserve the source extension, and skip placeholder output when there are no narrations. Add unit tests proving deterministic poster clamping, deduplicated audio copies, and no placeholder audio behavior.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/output/capture-poster.test.ts packages/generator-playwright/src/output/export-audio.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/generator-playwright/src/output/capture-poster.ts` exports `PORTABLE_POSTER_TIMESTAMP_MS = 1_000`.
    - `packages/generator-playwright/src/output/export-audio.ts` writes under `audio/` and preserves the source extension.
    - Tests cover deterministic poster clamping and no-placeholder audio behavior.
  </acceptance_criteria>
  <done>The output layer can now create deterministic poster and portable audio artifacts independently of manifest assembly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assemble the validated portable manifest from final output artifacts</name>
  <files>packages/generator-playwright/package.json, packages/generator-playwright/src/output/write-generation-output.ts, packages/generator-playwright/src/output/write-generation-output.test.ts</files>
  <read_first>
    - packages/generator-playwright/package.json
    - packages/generator-playwright/src/output/write-generation-output.ts
    - packages/generator-playwright/src/output/write-generation-output.test.ts
    - packages/manifest/src/index.ts
    - packages/generator-playwright/src/execute/generator-types.ts
    - .planning/phases/05-portable-output-contract/05-CONTEXT.md
    - .planning/phases/05-portable-output-contract/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Successful output writing now persists `manifest.json`, `poster.jpg`, `chapters.json`, `captions.srt`, `captions.vtt`, `video.mp4`, copied `audio/` files, and optional `video.webm`.
    - Test 2: `manifest.json` parses through `parsePortableOutputManifest()` and contains only output-root-relative POSIX paths, never cache-root or repo-root absolute paths.
    - Test 3: Manifest descriptors are built from final artifact bytes after poster/audio export completes, so checksum metadata reflects the portable output directory only.
  </behavior>
  <action>Add `@demohunter/manifest` as a workspace dependency in `packages/generator-playwright/package.json`, then update `packages/generator-playwright/src/output/write-generation-output.ts` to keep the existing `chapters.json` and caption behavior while adding `poster.jpg`, copied `audio/` artifacts, and `manifest.json` assembly per D-01 through D-05 and D-08 through D-10. Build every file descriptor with `createPortableArtifactDescriptor()`, validate the finished object with `parsePortableOutputManifest()`, and write `manifest.json` only after `video.mp4`, optional `video.webm`, `poster.jpg`, `chapters.json`, `captions.srt`, `captions.vtt`, and `audio/` exports are finalized. Do not include `baseURL`, `configPath`, cache-root paths, temp recording paths, or any other machine-local values in the manifest. Update `packages/generator-playwright/src/output/write-generation-output.test.ts` to assert the full portable artifact set and the strict manifest shape.</action>
  <verify>
    <automated>bun test packages/generator-playwright/src/output/write-generation-output.test.ts</automated>
    <automated>bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/generator-playwright/package.json` declares `@demohunter/manifest` as a workspace dependency.
    - `packages/generator-playwright/src/output/write-generation-output.ts` contains `manifest.json`, `poster.jpg`, `audio`, `createPortableArtifactDescriptor(`, and `parsePortableOutputManifest(`.
    - `packages/generator-playwright/src/output/write-generation-output.test.ts` expects `manifest.json`, `poster.jpg`, copied `audio/`, and required `video.mp4` with optional `video.webm`.
  </acceptance_criteria>
  <done>The generator output writer now emits the full portable Phase 5 artifact set and validates the manifest against the authoritative package contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final video file -> poster helper | FFmpeg and ffprobe output become user-visible portable artifacts |
| cache audio paths -> output `audio/` copies | Cache-local narration files cross into the external portable contract |
| final portable files -> manifest descriptors | Exported bytes become the integrity metadata future ingest will trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | T | `packages/generator-playwright/src/output/capture-poster.ts` | mitigate | Use the fixed `1_000` ms poster constant plus deterministic clamping so reruns cannot drift to heuristic poster selection per D-06 and D-07. |
| T-05-03-02 | D | `packages/generator-playwright/src/output/export-audio.ts` | mitigate | Copy unique narration files once into `audio/` and omit placeholder output when no narrations exist so repeated or empty runs cannot corrupt the portable artifact set per D-04 and D-05. |
| T-05-03-03 | I | `packages/generator-playwright/src/output/write-generation-output.ts` | mitigate | Build every manifest path from `createPortableArtifactDescriptor()` so cache-root, repo-root, temp, and secret-bearing values never enter `manifest.json` per D-03 and D-09. |
| T-05-03-04 | T | `packages/generator-playwright/src/output/write-generation-output.ts` | mitigate | Validate the completed manifest with `parsePortableOutputManifest()` only after final artifacts exist so checksum and relationship data cannot drift from exported bytes per OUT-02 and OUT-04. |
</threat_model>

<verification>
- `bun test packages/generator-playwright/src/output/capture-poster.test.ts packages/generator-playwright/src/output/export-audio.test.ts`
- `bun test packages/generator-playwright/src/output/write-generation-output.test.ts`
- `bun x tsc -b packages/generator-playwright/tsconfig.json --pretty false`
</verification>

<success_criteria>
The output writer now produces `poster.jpg`, copied `audio/`, and a validated portable manifest from final exported bytes, with only portable relative paths crossing the package boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/05-portable-output-contract/05-portable-output-contract-03-SUMMARY.md`
</output>
