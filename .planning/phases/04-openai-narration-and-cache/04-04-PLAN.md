---
phase: 04-openai-narration-and-cache
plan: 04
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
files_modified:
  - packages/cli/src/bin/demohunter.ts
  - packages/cli/src/bin/demohunter.test.ts
  - packages/cli/src/commands/cache.ts
  - packages/cli/src/commands/cache.test.ts
  - tests/fixtures/tours/phase-04-narration.tour.ts
  - tests/e2e/generation-engine-contract.test.ts
  - tests/e2e/built-cli-bin-contract.test.ts
autonomous: true
requirements:
  - TTS-07
  - TTS-08
  - TTS-09
user_setup: []
must_haves:
  truths:
    - "Developers can inspect the local narration cache with `demohunter cache list`, clear it with `clear`, and safely clean invalid entries with conservative `prune`."
    - "A fully cached narration run succeeds without `OPENAI_API_KEY`, while an uncached run fails clearly when the key is missing."
    - "Corrupt cache entries are recoverable through normal generation and cache maintenance flows rather than requiring manual repair."
  artifacts:
    - path: packages/cli/src/commands/cache.ts
      provides: "CLI cache command wrapper over shared cache primitives"
      contains: "cacheCommand"
    - path: tests/e2e/generation-engine-contract.test.ts
      provides: "Source-level Phase 4 narration/cache contract proof"
      contains: "captions.srt"
    - path: tests/e2e/built-cli-bin-contract.test.ts
      provides: "Built CLI narration/cache contract proof"
      contains: "cache"
  key_links:
    - from: packages/cli/src/bin/demohunter.ts
      to: packages/cli/src/commands/cache.ts
      via: "CLI subcommand dispatch"
      pattern: "cache"
    - from: packages/cli/src/commands/cache.ts
      to: packages/tts-core/src/cache/cache-maintenance.ts
      via: "shared local cache maintenance helpers"
      pattern: "list|prune|clear"
    - from: tests/e2e/generation-engine-contract.test.ts
      to: packages/cli/src/bin/demohunter.ts
      via: "source CLI offline/corrupt-cache validation"
      pattern: "OPENAI_API_KEY|cache"
---

<objective>
Wire the Phase 4 cache commands into the CLI and prove offline regeneration, missing-key failures, and corrupt-cache recovery through source and built contract tests.

Purpose: finish the user-facing cache lifecycle surface without turning it into a framework, and verify the local-first narration contract end to end.
Output: CLI `cache` commands plus source/dist e2e coverage for offline, missing-key, and corrupt-cache behaviors.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-openai-narration-and-cache/04-CONTEXT.md
@.planning/phases/04-openai-narration-and-cache/04-RESEARCH.md
@.planning/phases/03-playwright-generation-engine/03-VERIFICATION.md
@.planning/phases/04-openai-narration-and-cache/04-02-PLAN.md
@.planning/phases/04-openai-narration-and-cache/04-03-PLAN.md
@packages/cli/src/bin/demohunter.ts
@packages/cli/src/bin/demohunter.test.ts
@packages/cli/src/commands/generate.ts
@tests/e2e/generation-engine-contract.test.ts
@tests/e2e/built-cli-bin-contract.test.ts

<interfaces>
Current CLI dispatch is flat:

```typescript
switch (command) {
  case "init":
  case "generate":
}
```

Plan 04 should extend that without duplicating cache logic in the CLI. The command layer should remain a thin wrapper over `tts-core` maintenance helpers:

```typescript
await cacheCommand(cwd, { action: "list" | "prune" | "clear" });
```

The end-to-end fixtures should prove `captions.srt` and `captions.vtt` now exist for successful narrated runs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add minimal local-first cache commands to the CLI</name>
  <files>packages/cli/src/bin/demohunter.ts, packages/cli/src/bin/demohunter.test.ts, packages/cli/src/commands/cache.ts, packages/cli/src/commands/cache.test.ts</files>
  <read_first>
    - packages/cli/src/bin/demohunter.ts
    - packages/cli/src/bin/demohunter.test.ts
    - packages/cli/src/commands/generate.ts
    - packages/tts-core/src/cache/cache-maintenance.ts
    - .planning/phases/04-openai-narration-and-cache/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `demohunter cache list` prints inspectable local cache entries derived from shared maintenance helpers.
    - Test 2: `demohunter cache prune` performs conservative cleanup only, returning a local summary instead of a framework-style management UI.
    - Test 3: `demohunter cache clear` removes the narration cache root and updates CLI usage errors to include the new `cache` subcommand shape.
  </behavior>
  <action>Create `packages/cli/src/commands/cache.ts` and keep it as a thin wrapper over the shared `tts-core` maintenance helpers from Plan 04-02. Extend `packages/cli/src/bin/demohunter.ts` and its tests so `demohunter cache list`, `demohunter cache prune`, and `demohunter cache clear` are dispatched explicitly. Reference D-09 by keeping the command surface minimal, local-only, and inspectable. Reference D-10 by ensuring `prune` is still conservative because it delegates to the shared maintenance policy rather than inventing a separate CLI deletion strategy. Do not add interactive prompts, remote cache endpoints, or a general cache-management framework.</action>
  <verify>
    <automated>bun test packages/cli/src/bin/demohunter.test.ts packages/cli/src/commands/cache.test.ts</automated>
    <automated>bun x tsc -b packages/cli/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - The CLI supports `cache list`, `cache prune`, and `cache clear` as first-class subcommands.
    - Cache commands delegate to shared `tts-core` rules instead of duplicating filesystem policy.
    - Usage strings and tests reflect the expanded CLI surface cleanly.
  </acceptance_criteria>
  <done>The Phase 4 cache lifecycle surface is available from the CLI without broadening beyond the three required local-only commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove Phase 4 narration and offline behavior through the source CLI contract</name>
  <files>tests/fixtures/tours/phase-04-narration.tour.ts, tests/e2e/generation-engine-contract.test.ts</files>
  <read_first>
    - tests/e2e/generation-engine-contract.test.ts
    - tests/fixtures/tours/phase-03-generation.tour.ts
    - packages/cli/src/bin/demohunter.ts
    - packages/generator-playwright/src/generate.ts
    - .planning/phases/04-openai-narration-and-cache/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: A successful narrated source-CLI run produces `video.mp4`, `chapters.json`, `captions.srt`, and `captions.vtt`.
    - Test 2: After priming the cache once, rerunning the same tour without `OPENAI_API_KEY` succeeds from local cache only.
    - Test 3: An uncached run without `OPENAI_API_KEY` fails clearly, and a deliberately corrupted cache entry is recovered by normal regeneration or conservative prune plus rerun.
  </behavior>
  <action>Create `tests/fixtures/tours/phase-04-narration.tour.ts` as a representative narrated fixture with multiple narration segments so cache reuse, timing, and subtitle generation are meaningful. Update `tests/e2e/generation-engine-contract.test.ts` to prove the Phase 4 source-level contract end to end: successful narrated output now includes captions, fully cached reruns succeed with the env key removed per D-02, uncached missing-key runs fail clearly per TTS-08, and corrupt cache data is recoverable per D-06. The e2e assertions should also exercise `demohunter cache list`, `prune`, and `clear` so the CLI behavior is proven against real on-disk state, not just unit mocks.</action>
  <verify>
    <automated>bun test tests/e2e/generation-engine-contract.test.ts</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - Source-level e2e coverage proves narrated output, cache reuse, offline reruns, missing-key failure, corrupt-cache recovery, and CLI cache commands.
    - Successful runs now assert caption artifacts in addition to the existing Phase 3 output.
    - Failure-path assertions remain clear and do not claim success when narration cannot be resolved.
  </acceptance_criteria>
  <done>The Phase 4 source CLI contract is proven against the real local cache and narration behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the same cache and offline contract through the built CLI bin</name>
  <files>tests/e2e/built-cli-bin-contract.test.ts</files>
  <read_first>
    - tests/e2e/built-cli-bin-contract.test.ts
    - tests/fixtures/tours/phase-04-narration.tour.ts
    - packages/cli/dist/bin/demohunter.js
    - packages/cli/src/bin/demohunter.ts
  </read_first>
  <behavior>
    - Test 1: The compiled CLI can generate a narrated fixture and emit caption artifacts from a fresh temp repo.
    - Test 2: The compiled CLI can list, prune, and clear the local cache using the same local-only behavior as the source path.
    - Test 3: Built-output contract coverage includes offline cached reruns and uncached missing-key failure behavior, not just the happy path.
  </behavior>
  <action>Extend `tests/e2e/built-cli-bin-contract.test.ts` so the compiled dist entrypoint proves the same Phase 4 contract as the source CLI path. Rebuild the workspace inside the test as current convention requires, then assert narrated output artifacts, caption files, cache command behavior, offline cached reruns, and clear missing-key failures from a fresh temp consumer repo. Keep the fixture and verification local-first: no remote cache, no cloud upload, and no credential storage outside environment variables.</action>
  <verify>
    <automated>bun test tests/e2e/built-cli-bin-contract.test.ts</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - The built CLI path proves the same narration/cache contract as the source CLI path.
    - Fresh-temp-repo verification still uses the real compiled `demohunter` bin rather than source imports.
    - Caption artifacts and cache lifecycle behavior are covered in the built contract, not just generation success.
  </acceptance_criteria>
  <done>Phase 4’s user-facing narration and cache behavior is proven through both source and compiled CLI entrypoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI input -> cache commands | Users can trigger destructive local cache operations through the CLI |
| real cache state -> e2e verification | Tests depend on actual on-disk cache contents and corruption scenarios |
| compiled CLI -> consumer repo | Dist output must preserve the same local-only behavior as source code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | T | CLI cache operations | mitigate | Dispatch `list`, `prune`, and `clear` through the shared `tts-core` maintenance helpers so destructive behavior follows one conservative policy per D-09 and D-10. |
| T-04-04-02 | R | offline regeneration guarantee | mitigate | Add source and built e2e tests that explicitly remove `OPENAI_API_KEY` after priming cache and prove reruns succeed only when all narration segments are cached. |
| T-04-04-03 | D | corrupt cache handling | mitigate | Include corruption-path e2e coverage so broken metadata/audio becomes a recoverable miss instead of a permanent generation failure per D-06. |
</threat_model>

<verification>
- `bun test packages/cli/src/bin/demohunter.test.ts packages/cli/src/commands/cache.test.ts`
- `bun test tests/e2e/generation-engine-contract.test.ts`
- `bun test tests/e2e/built-cli-bin-contract.test.ts`
- `bun x tsc -b tsconfig.json --pretty false`
</verification>

<success_criteria>
The CLI exposes the required local cache lifecycle commands and the repo proves, through both source and built contracts, that narrated generation, offline cache reuse, missing-key failure, and corrupt-cache recovery all behave as intended.
</success_criteria>

<output>
After completion, create `.planning/phases/04-openai-narration-and-cache/04-openai-narration-and-cache-04-SUMMARY.md`
</output>
