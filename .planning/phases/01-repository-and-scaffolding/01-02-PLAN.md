---
phase: 01-repository-and-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tsconfig.json
  - packages/tts-core/package.json
  - packages/tts-core/tsconfig.json
  - packages/tts-core/src/index.ts
  - packages/tts-openai/package.json
  - packages/tts-openai/tsconfig.json
  - packages/tts-openai/src/index.ts
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/src/index.ts
  - packages/cli/src/bin/demohunter.ts
  - packages/create-demohunter/package.json
  - packages/create-demohunter/tsconfig.json
  - packages/create-demohunter/src/index.ts
autonomous: true
requirements:
  - INIT-02
user_setup: []
must_haves:
  truths:
    - "All seven planned Phase 1 package boundaries exist by the end of repository setup per D-01 and D-02."
    - "The CLI-facing packages exist without requiring placeholder examples, placeholder skills, or cloud behavior."
    - "Later Phase 1 plans can implement config loading and scaffolding against stable package names and TypeScript references."
  artifacts:
    - path: "packages/tts-core/package.json"
      provides: "TTS core package shell"
      contains: "\"name\": \"@demohunter/tts-core\""
    - path: "packages/tts-openai/package.json"
      provides: "OpenAI TTS package shell"
      contains: "\"name\": \"@demohunter/tts-openai\""
    - path: "packages/cli/package.json"
      provides: "CLI package shell with a compiled bin contract"
      contains: "\"./dist/bin/demohunter.js\""
    - path: "packages/create-demohunter/package.json"
      provides: "Scaffold package shell"
      contains: "\"name\": \"create-demohunter\""
  key_links:
    - from: "tsconfig.json"
      to: "packages/tts-core/tsconfig.json"
      via: "project references"
      pattern: "packages/tts-core"
    - from: "tsconfig.json"
      to: "packages/cli/tsconfig.json"
      via: "project references"
      pattern: "packages/cli"
    - from: "packages/cli/package.json"
      to: "packages/cli/src/bin/demohunter.ts"
      via: "CLI source path compiles to the declared dist bin"
      pattern: "\"./dist/bin/demohunter.js\""
    - from: "packages/create-demohunter/package.json"
      to: "packages/create-demohunter/src/index.ts"
      via: "package entry"
      pattern: "\"exports\""
---

<objective>
Finish the Phase 1 package-shell layout without expanding scope beyond the repository foundation.

Purpose: complete the remaining package boundaries needed for config loading, scaffolding, and later TTS work while keeping Phase 1 setup local-first and minimal.
Output: thin shells for `@demohunter/tts-core`, `@demohunter/tts-openai`, `demohunter`, and `create-demohunter`, plus updated root references.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-and-scaffolding/01-CONTEXT.md
@docs/phase_1_oss_core.md
@.planning/phases/01-repository-and-scaffolding/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the remaining package shells for TTS and scaffold flows</name>
  <files>packages/tts-core/package.json, packages/tts-core/tsconfig.json, packages/tts-core/src/index.ts, packages/tts-openai/package.json, packages/tts-openai/tsconfig.json, packages/tts-openai/src/index.ts, packages/create-demohunter/package.json, packages/create-demohunter/tsconfig.json, packages/create-demohunter/src/index.ts</files>
  <action>Create package shells for `@demohunter/tts-core`, `@demohunter/tts-openai`, and `create-demohunter` using the same ESM/build conventions as Plan 01. Keep them intentionally thin per D-01 and D-02: package metadata, `exports`, `main`, `types`, local `build` / `typecheck` scripts, and minimal `src/index.ts` entrypoints. Each package `tsconfig.json` must explicitly extend `../../tsconfig.base.json` and pin the per-package emit layout used by later dist-first plans: `rootDir: "./src"`, `outDir: "./dist"`, and declaration output kept under `dist` with dist-compatible settings such as `declaration: true`, `declarationMap: true`, and `declarationDir: "./dist"`. Do not create placeholder skill directories, placeholder example apps, or any runtime credential logic in this plan.</action>
  <verify>
    <automated>rg -n '"name": "@demohunter/(tts-core|tts-openai)"|"name": "create-demohunter"|"build": "tsc -p tsconfig.json"|"type": "module"|"main": "\\./dist/index\\.js"|"types": "\\./dist/index\\.d\\.ts"' packages/{tts-core,tts-openai,create-demohunter}/package.json && rg -n '"rootDir": "\\./src"|"outDir": "\\./dist"|"declarationDir": "\\./dist"' packages/{tts-core,tts-openai,create-demohunter}/tsconfig.json && ! rg -n 'OPENAI_API_KEY|examples/|skills/' packages/{tts-core,tts-openai,create-demohunter}/{package.json,src/index.ts}</automated>
  </verify>
  <done>The TTS and scaffold packages exist as thin shells with no placeholder directories or cloud-specific setup.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CLI package shell and extend root project references</name>
  <files>tsconfig.json, packages/cli/package.json, packages/cli/tsconfig.json, packages/cli/src/index.ts, packages/cli/src/bin/demohunter.ts</files>
  <action>Create the `demohunter` CLI package shell with the same ESM/build conventions and make the compiled CLI contract explicit in this shell plan. `packages/cli/package.json` must declare `"bin": { "demohunter": "./dist/bin/demohunter.js" }` so later plans target the real built entrypoint, not an implied path. `packages/cli/tsconfig.json` must explicitly extend `../../tsconfig.base.json` and set `rootDir: "./src"`, `outDir: "./dist"`, and dist-compatible declaration output under `./dist`. Create both `packages/cli/src/index.ts` and a placeholder executable source file at `packages/cli/src/bin/demohunter.ts` so the package has a concrete source path contract that compiles to `dist/bin/demohunter.js` before Plan 01-04 replaces the stub with real command handling. Update root `tsconfig.json` to add project references for the packages introduced in this plan and preserve the references from Plan 01. Keep the CLI shell limited to package metadata and placeholder source files; do not implement command behavior here.</action>
  <verify>
    <automated>test -f packages/cli/src/bin/demohunter.ts && rg -n '"name": "demohunter"|"type": "module"|"bin"|"demohunter": "\\./dist/bin/demohunter\\.js"|"main": "\\./dist/index\\.js"|"types": "\\./dist/index\\.d\\.ts"' packages/cli/package.json && rg -n '"rootDir": "\\./src"|"outDir": "\\./dist"|"declarationDir": "\\./dist"' packages/cli/tsconfig.json && rg -n 'packages/(sdk|generator-playwright|manifest|tts-core|tts-openai|cli|create-demohunter)' tsconfig.json</automated>
  </verify>
  <done>All seven planned package boundaries exist and the root TypeScript graph points at each Phase 1 package shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root config -> added package refs | The root project graph now includes packages that later plans will execute from |
| package metadata -> developer shell | CLI and scaffold package metadata expose executable entrypoints in future plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | package manifests | mitigate | Keep package manifests thin and exclude credential handling, sample content, and unrelated runtime dependencies from this shell-only plan. |
| T-01-02-02 | E | future CLI bin metadata | mitigate | Add metadata only; defer executable behavior to the later dedicated CLI plan so shell creation does not implicitly run commands. |
| T-01-02-03 | T | root `tsconfig.json` references | mitigate | Append new references without altering the root compiler contract established in Plan 01. |
</threat_model>

<verification>
Run the task-level `rg` checks, then run `bun x tsc -b tsconfig.json --pretty false` after Plans 01 and 02 complete to confirm the full package-shell graph builds cleanly.
</verification>

<success_criteria>
The repository now contains all seven Phase 1 package shells and the full workspace layout, still with no sample-app placeholders or cloud-side behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-and-scaffolding/01-02-SUMMARY.md`
</output>
