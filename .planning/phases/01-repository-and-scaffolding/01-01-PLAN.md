---
phase: 01-repository-and-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bunfig.toml
  - tsconfig.base.json
  - tsconfig.json
  - packages/sdk/package.json
  - packages/sdk/tsconfig.json
  - packages/sdk/src/index.ts
  - packages/generator-playwright/package.json
  - packages/generator-playwright/tsconfig.json
  - packages/generator-playwright/src/index.ts
  - packages/manifest/package.json
  - packages/manifest/tsconfig.json
  - packages/manifest/src/index.ts
autonomous: true
requirements:
  - INIT-02
user_setup: []
must_haves:
  truths:
    - "The repo installs as a Bun workspace with strict ESM TypeScript settings and no cloud-only dependency in the default setup."
    - "Phase 1 has stable package shells for the SDK, Playwright generator, and manifest packages per D-01 and D-02."
    - "Later Phase 1 plans can add behavior without reshaping the workspace root."
  artifacts:
    - path: "package.json"
      provides: "Root Bun workspace definition and shared scripts"
      contains: "\"workspaces\""
    - path: "tsconfig.base.json"
      provides: "Shared strict ESM TypeScript settings"
      contains: "\"module\": \"NodeNext\""
    - path: "packages/sdk/package.json"
      provides: "SDK package shell"
      contains: "\"name\": \"@demohunter/sdk\""
    - path: "packages/generator-playwright/package.json"
      provides: "Generator package shell with the Phase 1 Playwright floor"
      contains: "\"playwright\": \">=1.59\""
    - path: "packages/manifest/package.json"
      provides: "Manifest package shell"
      contains: "\"name\": \"@demohunter/manifest\""
  key_links:
    - from: "package.json"
      to: "packages/*/package.json"
      via: "Bun workspaces"
      pattern: "\"workspaces\""
    - from: "tsconfig.base.json"
      to: "packages/*/tsconfig.json"
      via: "shared compiler options"
      pattern: "\"extends\": \"../../tsconfig.base.json\""
    - from: "packages/generator-playwright/tsconfig.json"
      to: "packages/generator-playwright/package.json"
      via: "dist emit layout matches the declared package entrypoints"
      pattern: "\"outDir\": \"./dist\""
    - from: "tsconfig.json"
      to: "packages/sdk/tsconfig.json"
      via: "project references"
      pattern: "packages/sdk"
---

<objective>
Create the root workspace contract and the first package-shell group for Phase 1.

Purpose: establish the Bun/TypeScript/ESM foundation plus the first three package boundaries so later plans can add behavior without revisiting repository shape.
Output: root workspace config and thin shells for `@demohunter/sdk`, `@demohunter/generator-playwright`, and `@demohunter/manifest`.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-repository-and-scaffolding/01-CONTEXT.md
@docs/GOAL.md
@docs/INITIAL_RESEARCH.md
@docs/phase_1_oss_core.md
@docs/phase_2_cloud_offering.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root Bun workspace and shared TypeScript contract</name>
  <files>package.json, bunfig.toml, tsconfig.base.json, tsconfig.json</files>
  <action>Create a root `package.json` with `name: "demohunter"`, `private: true`, `type: "module"`, `workspaces: ["packages/*"]`, and local-only scripts `build`, `typecheck`, and `test`. Create `bunfig.toml` with `[install] linker = "isolated"` so package resolution is deterministic. Create `tsconfig.base.json` with strict ESM settings: `target: "ES2022"`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `strict: true`, `verbatimModuleSyntax: true`, `declaration: true`, `declarationMap: true`, `sourceMap: true`, `resolveJsonModule: true`, and `noEmitOnError: true`. Create root `tsconfig.json` with TypeScript project references for only the packages introduced in this plan; later plans may append references, but must not change the root contract.</action>
  <verify>
    <automated>rg -n '"workspaces"|"type": "module"' package.json && rg -n 'linker = "isolated"' bunfig.toml && rg -n '"module": "NodeNext"|"verbatimModuleSyntax": true' tsconfig.base.json && rg -n 'packages/(sdk|generator-playwright|manifest)' tsconfig.json</automated>
  </verify>
  <done>The repo has a Bun workspace root and shared strict TypeScript settings that later plans can extend without reworking the workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Create the first package-shell group per D-01 and D-02</name>
  <files>packages/sdk/package.json, packages/sdk/tsconfig.json, packages/sdk/src/index.ts, packages/generator-playwright/package.json, packages/generator-playwright/tsconfig.json, packages/generator-playwright/src/index.ts, packages/manifest/package.json, packages/manifest/tsconfig.json, packages/manifest/src/index.ts</files>
  <action>Create thin package shells for `@demohunter/sdk`, `@demohunter/generator-playwright`, and `@demohunter/manifest` per D-01 and D-02. Each package must use `type: "module"`, export `"."`, set `main: "./dist/index.js"` and `types: "./dist/index.d.ts"`, and define `build` / `typecheck` scripts with `tsc -p tsconfig.json`. Each package `tsconfig.json` must explicitly extend `../../tsconfig.base.json` and pin the per-package emit layout that later dist-first plans depend on: `rootDir: "./src"`, `outDir: "./dist"`, and declaration output kept under `dist` with dist-compatible declaration settings such as `declaration: true`, `declarationMap: true`, and `declarationDir: "./dist"`. Add minimal `src/index.ts` placeholder exports that compile cleanly. In `packages/generator-playwright/package.json`, make `playwright` with version `>=1.59` a mandatory dependency because this package will own `smokeGenerate` in Plan 01-04; do not use `@playwright/test`. Keep all other dependencies minimal and do not add OpenAI, Zod, ffmpeg, or any cloud-specific runtime dependency in this plan.</action>
  <verify>
    <automated>rg -n '"name": "@demohunter/(sdk|generator-playwright|manifest)"|"build": "tsc -p tsconfig.json"|"type": "module"|"main": "\\./dist/index\\.js"|"types": "\\./dist/index\\.d\\.ts"' packages/{sdk,generator-playwright,manifest}/package.json && rg -n '"rootDir": "\\./src"|"outDir": "\\./dist"|"declarationDir": "\\./dist"' packages/{sdk,generator-playwright,manifest}/tsconfig.json && rg -n '"playwright": ">=1\\.59"' packages/generator-playwright/package.json && ! rg -n '"@playwright/test"|"openai"|"zod"|"ffmpeg"' packages/{sdk,generator-playwright,manifest}/package.json</automated>
  </verify>
  <done>The first package group exists as stable, compilable shells with no cloud leakage and no future-phase dependency creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> package manager | Third-party packages and scripts enter the local toolchain |
| root config -> package builds | Shared compiler settings affect every package in the workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | T | root `package.json` dependencies | mitigate | Keep manifests minimal and exclude OpenAI, Zod, ffmpeg, and other unused runtime dependencies from this plan. |
| T-01-01-02 | T | shared TypeScript config | mitigate | Check in explicit `NodeNext` / ESM compiler settings rather than relying on implicit defaults or per-package drift. |
| T-01-01-03 | E | package-shell scripts | accept | Package-shell scripts only call local `tsc` and do not grant elevated behavior in this plan. |
</threat_model>

<verification>
Run the task-level `rg` checks, then run `bun x tsc -b tsconfig.json --pretty false` once both tasks complete to confirm the root workspace and first package shells are internally consistent.
</verification>

<success_criteria>
The repository has a stable Bun/ESM workspace root and the first three Phase 1 package boundaries without any cloud-specific behavior or placeholder extras.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-and-scaffolding/01-01-SUMMARY.md`
</output>
