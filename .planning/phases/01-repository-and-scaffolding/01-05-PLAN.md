---
phase: 01-repository-and-scaffolding
plan: 05
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-04
files_modified:
  - tests/e2e/workspace-build-contract.test.ts
  - tests/e2e/built-cli-bin-contract.test.ts
autonomous: true
requirements:
  - INIT-01
  - INIT-02
user_setup: []
must_haves:
  truths:
    - "After a workspace build, DemoHunter's compiled dist entrypoints resolve and execute without relying on Bun source transpilation."
    - "The built `demohunter` CLI/bin surface can run `init` and `generate` in a temp repo, not just the source entrypoint."
    - "Cross-package wiring between the compiled SDK, scaffold package, generator package, and CLI is proven by automated dist-first checks."
  artifacts:
    - path: "tests/e2e/workspace-build-contract.test.ts"
      provides: "Build-first workspace export contract"
      contains: "bun x tsc -b tsconfig.json --pretty false"
    - path: "tests/e2e/built-cli-bin-contract.test.ts"
      provides: "Compiled CLI/bin end-to-end contract"
      contains: "packages/cli/dist/bin/demohunter.js"
  key_links:
    - from: "tests/e2e/workspace-build-contract.test.ts"
      to: "packages/*/dist/index.js"
      via: "compiled ESM import checks"
      pattern: "dist/index.js"
    - from: "tests/e2e/built-cli-bin-contract.test.ts"
      to: "packages/cli/dist/bin/demohunter.js"
      via: "spawn built bin in temp repo"
      pattern: "packages/cli/dist/bin/demohunter.js"
---

<objective>
Make the workspace build contract explicit and prove the real compiled CLI/bin surface after the Phase 1 setup plans land.

Purpose: close the source-vs-dist gap called out by the checker by requiring a post-change workspace build and validating the compiled package entrypoints and CLI/bin flow, not just Bun source execution.
Output: build-first contract tests for compiled package exports and the built `demohunter` bin.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-and-scaffolding/01-CONTEXT.md
@.planning/phases/01-repository-and-scaffolding/01-03-PLAN.md
@.planning/phases/01-repository-and-scaffolding/01-04-PLAN.md

<interfaces>
Compiled entrypoints that must be proven after `bun x tsc -b tsconfig.json --pretty false`:

```ts
packages/sdk/dist/index.js
packages/generator-playwright/dist/index.js
packages/create-demohunter/dist/index.js
packages/cli/dist/bin/demohunter.js
```

The built bin contract for this phase is:

```sh
bun packages/cli/dist/bin/demohunter.js init
bun packages/cli/dist/bin/demohunter.js generate demos/sample.tour.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a build-first workspace export contract test</name>
  <files>tests/e2e/workspace-build-contract.test.ts</files>
  <behavior>- Test 1: The test runs `bun x tsc -b tsconfig.json --pretty false` from the repo root before any dist assertions.
- Test 2: The build emits the expected compiled entrypoints for the SDK, generator, scaffold package, and CLI bin.
- Test 3: A clean ESM subprocess can import the built `packages/sdk/dist/index.js`, `packages/generator-playwright/dist/index.js`, and `packages/create-demohunter/dist/index.js` entrypoints and observe the expected named exports.</behavior>
  <action>Create `tests/e2e/workspace-build-contract.test.ts` with Bun's test runner. The test must shell out to `bun x tsc -b tsconfig.json --pretty false`, assert that the expected `dist/index.js` files and `packages/cli/dist/bin/demohunter.js` exist afterward, and then use a subprocess with file-URL imports to load the compiled ESM entrypoints directly from `dist/`. Check at minimum that the built SDK exports `defineConfig` and `defineTour`, the built generator exports `smokeGenerate`, and the built scaffold package exports the scaffold entrypoint created in Plan 01-04. Do not import any `src/` file or rely on Bun's TypeScript transpilation in this test.</action>
  <verify>
    <automated>bun test tests/e2e/workspace-build-contract.test.ts</automated>
  </verify>
  <done>The workspace has an explicit build-first contract proving the compiled package entrypoints exist and can be imported directly from `dist/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the compiled `demohunter` bin in a temp repo</name>
  <files>tests/e2e/built-cli-bin-contract.test.ts</files>
  <behavior>- Test 1: After a workspace build, a temp repo can run `bun packages/cli/dist/bin/demohunter.js init` and receive the three starter files.
- Test 2: The same temp repo can run `bun packages/cli/dist/bin/demohunter.js generate demos/sample.tour.ts` and produce `.demohunter/sample-smoke/smoke-run.json`.
- Test 3: The built-bin flow asserts the generated JSON contains `"status":"ok"` and the resolved browser/viewport fields, proving the compiled CLI, loader, and generator packages work together.</behavior>
  <action>Create `tests/e2e/built-cli-bin-contract.test.ts` with Bun's test runner. The test must first run `bun x tsc -b tsconfig.json --pretty false`, then create a temporary target repo, and spawn the compiled bin at `packages/cli/dist/bin/demohunter.js` with `init` and `generate demos/sample.tour.ts`. Verify the three scaffolded starter files exist after `init`, then read `.demohunter/sample-smoke/smoke-run.json` after `generate` and assert it contains `"status":"ok"`, `"tourId":"sample-smoke"`, and the browser/viewport values loaded by the compiled config path. Do not invoke `packages/cli/src/bin/demohunter.ts` anywhere in this test, and do not treat a source-level run as sufficient proof.</action>
  <verify>
    <automated>bun test tests/e2e/built-cli-bin-contract.test.ts</automated>
  </verify>
  <done>The compiled `demohunter` bin is validated end-to-end in a temp repo, proving the authoritative Phase 1 user contract through built output rather than source execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source workspace -> compiled dist artifacts | The build process converts package source into the entrypoints users will actually execute |
| compiled bin -> temp repo filesystem | The built CLI writes starter files and generated artifacts into another repository |
| compiled package imports -> runtime execution | Dist entrypoints must resolve correctly without source-only conveniences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | T | compiled package entrypoints | mitigate | Require `bun x tsc -b tsconfig.json --pretty false` in the automated contract test and import built `dist/` files directly instead of trusting source execution. |
| T-01-05-02 | E | built CLI/bin surface | mitigate | Spawn only `packages/cli/dist/bin/demohunter.js` in the end-to-end test so the executable contract is proven from compiled output. |
| T-01-05-03 | T | temp repo artifact writes | mitigate | Reuse the safe scaffold and smoke-path assertions from prior plans and verify only the expected starter files plus `.demohunter/sample-smoke/smoke-run.json` are created. |
</threat_model>

<verification>
Run `bun test tests/e2e/workspace-build-contract.test.ts` and `bun test tests/e2e/built-cli-bin-contract.test.ts`. Both tests must perform `bun x tsc -b tsconfig.json --pretty false` before asserting on compiled entrypoints or invoking the CLI/bin.
</verification>

<success_criteria>
Phase 1 now has an explicit post-build contract: compiled package entrypoints import cleanly from `dist/`, and the built `demohunter` bin can scaffold and run the smoke sample in a temp repo without relying on source entrypoints.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-and-scaffolding/01-05-SUMMARY.md`
</output>
