---
phase: 02-tour-authoring-sdk
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - tests/fixtures/tours/phase-02-authoring.tour.ts
  - tests/e2e/authoring-sdk-contract.test.ts
  - tests/e2e/init-generate-smoke.test.ts
  - tests/e2e/workspace-build-contract.test.ts
  - tests/e2e/built-cli-bin-contract.test.ts
autonomous: true
requirements:
  - TOUR-01
  - TOUR-02
  - TOUR-03
  - TOUR-04
  - TOUR-05
must_haves:
  truths:
    - "A developer-consumable `defineTour` tour executes from a fresh temp repo after installing the local `@demohunter/sdk` package, through both source and built CLI contracts."
    - "The existing scaffolded plain-object starter still works, preserving the Phase 1 smoke/build path."
    - "Phase 2 validation proves helper-rich authored tours without requiring DemoHunter-managed auth or bootstrap abstractions."
  artifacts:
    - path: tests/fixtures/tours/phase-02-authoring.tour.ts
      provides: "Representative helper-rich Phase 2 tour fixture"
      contains: "defineTour"
    - path: tests/e2e/authoring-sdk-contract.test.ts
      provides: "Source-level end-to-end proof for Phase 2 authored tours"
      contains: "generate"
    - path: tests/e2e/built-cli-bin-contract.test.ts
      provides: "Compiled-bin proof that Phase 2 authoring and Phase 1 starter compatibility both hold"
      contains: "packages/cli/dist/bin/demohunter.js"
  key_links:
    - from: tests/e2e/authoring-sdk-contract.test.ts
      to: tests/fixtures/tours/phase-02-authoring.tour.ts
      via: "copies the representative tour into a fresh temp repo and runs CLI generate after installing local SDK dependencies"
      pattern: "phase-02-authoring.tour.ts|@demohunter/sdk"
    - from: tests/e2e/built-cli-bin-contract.test.ts
      to: packages/cli/dist/bin/demohunter.js
      via: "compiled CLI execution"
      pattern: "dist/bin/demohunter.js"
---

<objective>
Validate the Phase 2 authoring model with representative tours while preserving the current starter-tour and compiled-contract behavior from Phase 1.

Purpose: Close the compatibility gap between the richer SDK surface and the already-proven smoke/dist workflows.
Output: Representative fixtures and end-to-end tests that prove both new authoring capability and old starter compatibility.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tour-authoring-sdk/02-CONTEXT.md
@.planning/phases/02-tour-authoring-sdk/02-RESEARCH.md
@.planning/phases/01-repository-and-scaffolding/01-04-SUMMARY.md
@.planning/phases/01-repository-and-scaffolding/01-05-SUMMARY.md
@packages/create-demohunter/templates/starter/demos/sample.tour.ts
@tests/e2e/init-generate-smoke.test.ts
@tests/e2e/workspace-build-contract.test.ts
@tests/e2e/built-cli-bin-contract.test.ts

<interfaces>
Compatibility rule for this plan:
```typescript
// Must keep working
export default {
  id: "sample-smoke",
  title: "DemoHunter starter smoke test",
  async run({ page }) { /* ... */ },
};

// Must also work
export default defineTour({
  id: "phase-02-authoring",
  title: "Phase 2 authoring contract",
  async setup({ page, assertVisible }) { /* app auth/bootstrap stays here */ },
  async run({ page, chapter, step, narrate, waitForStable, highlight, snapshot }) { /* ... */ },
  async teardown({ page }) { /* cleanup */ },
});
```

The starter template remains plain-object in this phase unless the scaffold flow is also updated to make SDK imports resolvable in a fresh repo.

Fresh-repo authoring rule for TOUR-01:
```text
1. Create a temp repo with a minimal package.json.
2. Install local file dependencies on @demohunter/sdk and Playwright-compatible runtime packages from this workspace.
3. Copy in a `defineTour`-based `.tour.ts` file.
4. Run `demohunter generate` against that tour and assert the smoke artifact is produced.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add representative Phase 2 tour fixtures and source-level authoring-flow verification</name>
  <files>tests/fixtures/tours/phase-02-authoring.tour.ts, tests/e2e/authoring-sdk-contract.test.ts, tests/e2e/init-generate-smoke.test.ts</files>
  <read_first>
    - packages/create-demohunter/templates/starter/demos/sample.tour.ts
    - tests/e2e/init-generate-smoke.test.ts
    - packages/cli/src/commands/generate.ts
    - packages/generator-playwright/src/smoke-generate.ts
    - .planning/phases/02-tour-authoring-sdk/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: A fresh temp repo that installs the local `@demohunter/sdk` package can run a copied `defineTour` fixture using `setup`, `chapter`, `step`, `narrate`, `waitForStable`, `highlight`, `snapshot`, `assertVisible`, and `teardown` through the source CLI path.
    - Test 2: The existing `init -> generate` smoke test still passes with the scaffolded plain-object starter tour untouched.
    - Test 3: The representative fixture keeps auth/bootstrap logic in `setup` using ordinary Playwright page actions rather than DemoHunter abstractions.
  </behavior>
  <action>Create `tests/fixtures/tours/phase-02-authoring.tour.ts` as the representative authored tour for this phase. It should import `defineTour` from `@demohunter/sdk`, call every Phase 2 helper at least once, and keep bootstrap-style logic in `setup` via plain `page` operations to satisfy TOUR-05. Add `tests/e2e/authoring-sdk-contract.test.ts` that creates a fresh temporary repo, writes a minimal `package.json`, installs local file dependencies on `@demohunter/sdk` and the needed Playwright runtime packages from this workspace, copies the representative tour into that temp repo, and runs the source CLI or `generateCommand()` against the temp-repo tour path. Assert that `${config.outputDir}/phase-02-authoring/smoke-run.json` is produced in the temp repo, proving TOUR-01 on a real consumer-style path rather than a repo-only fixture. Update `tests/e2e/init-generate-smoke.test.ts` only as needed to explicitly assert that the current plain-object starter path still succeeds; do not convert the starter template to `defineTour` in this phase.</action>
  <verify>
    <automated>bun test tests/e2e/authoring-sdk-contract.test.ts tests/e2e/init-generate-smoke.test.ts</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/tours/phase-02-authoring.tour.ts` exercises `setup`, `run`, `teardown`, and every direct helper named in TOUR-04.
    - `tests/e2e/authoring-sdk-contract.test.ts` installs `@demohunter/sdk` into a fresh temp repo and proves `demohunter generate` succeeds there for a `defineTour`-based `.tour.ts` file.
    - The source-level authoring-flow test passes without changing the scaffolded starter tour file to import the SDK.
    - `tests/e2e/init-generate-smoke.test.ts` still verifies the Phase 1 starter path.
  </acceptance_criteria>
  <done>The repo has a representative Phase 2 authored tour and source-level proof that a developer can author and run a `defineTour` tour from a fresh temp repo without regressing the starter scaffold flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend built-output contract coverage for Phase 2 authoring and starter compatibility</name>
  <files>tests/e2e/workspace-build-contract.test.ts, tests/e2e/built-cli-bin-contract.test.ts</files>
  <read_first>
    - tests/e2e/workspace-build-contract.test.ts
    - tests/e2e/built-cli-bin-contract.test.ts
    - tests/fixtures/tours/phase-02-authoring.tour.ts
    - packages/sdk/src/index.ts
    - packages/cli/dist/bin/demohunter.js
  </read_first>
  <behavior>
    - Test 1: The workspace build contract still succeeds after the SDK and generator surface changes.
    - Test 2: The compiled CLI/bin can generate from a fresh temp repo containing a `defineTour` tour after that repo installs the local built SDK dependency path.
    - Test 3: The compiled CLI/bin still passes the scaffolded starter `init` + `generate` flow from Phase 1.
  </behavior>
  <action>Extend `tests/e2e/workspace-build-contract.test.ts` to assert the built SDK and generator entrypoints still import cleanly after the Phase 2 surface expansion. Update `tests/e2e/built-cli-bin-contract.test.ts` so it covers both paths in one suite: keep the existing temp-repo `init` + starter `generate` proof, and add a second compiled-bin flow that creates another fresh temp repo, installs local file dependencies for `@demohunter/sdk` and Playwright-compatible runtime packages from this workspace after the build, copies in the representative `defineTour` tour, and runs `packages/cli/dist/bin/demohunter.js generate <temp-repo-tour-path>`. Assert that the temp repo receives the expected `smoke-run.json` for `phase-02-authoring`. The built test should prove the Phase 2 authoring surface works from `dist` on a real consumer-style path while the starter plain-object contract still remains valid.</action>
  <verify>
    <automated>bun test tests/e2e/workspace-build-contract.test.ts tests/e2e/built-cli-bin-contract.test.ts</automated>
    <automated>bun x tsc -b tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - The compiled workspace still exposes the SDK, generator, scaffold, and CLI entrypoints from `dist`.
    - The built CLI can execute both the scaffolded starter tour and a fresh-temp-repo `defineTour` tour with installed local SDK dependencies.
    - The tests do not rely on stale build artifacts; they must trigger a fresh workspace build internally, following the Phase 1 pattern.
  </acceptance_criteria>
  <done>Phase 2 is proven at both source and built-contract levels, with explicit coverage for the new DSL surface on a real temp-repo consumer path and the old starter compatibility path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-controlled tour fixtures -> CLI/build tests | Test fixtures execute through the real CLI and must prove the intended surface without hiding regressions. |
| scaffolded temp repo -> compiled CLI | Fresh temp repos validate that DemoHunter still works when no workspace source imports are available. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | R | e2e contract tests | mitigate | Keep both source and built tests on real CLI entrypoints so regressions cannot be hidden by unit-only mocks. |
| T-02-03-02 | T | starter scaffold compatibility | mitigate | Preserve the plain-object starter path in dedicated e2e assertions instead of silently migrating it to SDK imports that a fresh repo cannot resolve. |
| T-02-03-03 | S | representative temp-repo authoring path | mitigate | Install only explicit local file dependencies for `@demohunter/sdk` and Playwright packages in temp repos, then run real CLI flows so TOUR-01 is proven without inventing framework-owned session helpers. |
</threat_model>

<verification>
- `bun test tests/e2e/authoring-sdk-contract.test.ts tests/e2e/init-generate-smoke.test.ts`
- `bun test tests/e2e/workspace-build-contract.test.ts tests/e2e/built-cli-bin-contract.test.ts`
- `bun x tsc -b tsconfig.json --pretty false`
</verification>

<success_criteria>
- The repository contains a representative Phase 2 `defineTour` fixture that exercises the full helper surface.
- Both source and built CLI paths can execute Phase 2 tours from a fresh temp-repo consumer path with local SDK installation.
- The starter plain-object tour and existing smoke/build contracts remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tour-authoring-sdk/02-tour-authoring-sdk-03-SUMMARY.md`
</output>
