---
phase: 02-tour-authoring-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sdk/package.json
  - packages/sdk/src/runtime-types.ts
  - packages/sdk/src/tour.ts
  - packages/sdk/src/index.ts
  - packages/sdk/src/tour.test.ts
autonomous: true
requirements:
  - TOUR-01
  - TOUR-02
  - TOUR-03
must_haves:
  truths:
    - "TypeScript authors can default export defineTour({ id, title, setup?, run, teardown? }) without a builder or fluent DSL."
    - "The SDK exposes Playwright-native runtime types for chapter, step, narrate, and helper calls directly on the run context."
    - "Phase 2 keeps lifecycle scope to top-level setup and teardown only."
  artifacts:
    - path: packages/sdk/src/runtime-types.ts
      provides: "Public runtime context and option-bag types for authored tours"
      exports:
        - DemoHunterLifecycleContext
        - DemoHunterRunContext
        - ChapterOptions
        - NarrateOptions
        - WaitForStableOptions
        - HighlightOptions
        - SnapshotOptions
        - AssertVisibleOptions
    - path: packages/sdk/src/tour.ts
      provides: "Identity-only defineTour contract with top-level lifecycle typing"
      contains: "export function defineTour"
    - path: packages/sdk/package.json
      provides: "SDK dependency contract for Playwright-authored tours"
      contains: "\"playwright\": \">=1.59\""
  key_links:
    - from: packages/sdk/src/tour.ts
      to: packages/sdk/src/runtime-types.ts
      via: "type imports for lifecycle and run contexts"
      pattern: "from \"./runtime-types.js\""
    - from: packages/sdk/src/index.ts
      to: packages/sdk/src/tour.ts
      via: "public SDK re-export"
      pattern: "from \"./tour.js\""
---

<objective>
Define the public Phase 2 authoring contract in `@demohunter/sdk` so tours stay plain `defineTour({ ... })` objects while gaining strong Playwright-native typing.

Purpose: Lock the authored `.tour.ts` surface before runtime work starts, per D-01 through D-09.
Output: Stable SDK types, exported contracts, and focused tests for the Phase 2 tour shape.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tour-authoring-sdk/02-CONTEXT.md
@.planning/phases/02-tour-authoring-sdk/02-RESEARCH.md
@docs/phase_1_oss_core.md
@packages/sdk/src/tour.ts
@packages/sdk/src/index.ts
@packages/generator-playwright/src/smoke-generate.ts

<interfaces>
From `packages/sdk/src/tour.ts` today:
```typescript
export type DemoHunterRunContext = {
  page: unknown;
  chapter: unknown;
  step: unknown;
  narrate: DemoHunterNarrate;
};

export type DemoHunterTour = {
  id: string;
  title: string;
  run: (context: DemoHunterRunContext) => Promise<void>;
};
```

Target contract for this plan:
```typescript
export type DemoHunterTour = {
  id: string;
  title: string;
  setup?: (context: DemoHunterLifecycleContext) => Promise<void> | void;
  run: (context: DemoHunterRunContext) => Promise<void> | void;
  teardown?: (context: DemoHunterLifecycleContext) => Promise<void> | void;
};
```

Use Playwright `Page` and `Locator` types directly. Do not introduce a DemoHunter-specific page or locator wrapper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Playwright-native runtime and tour contracts in the SDK</name>
  <files>packages/sdk/package.json, packages/sdk/src/runtime-types.ts, packages/sdk/src/tour.ts</files>
  <read_first>
    - packages/sdk/package.json
    - packages/sdk/src/tour.ts
    - packages/generator-playwright/src/smoke-generate.ts
    - docs/phase_1_oss_core.md
    - .planning/phases/02-tour-authoring-sdk/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `defineTour()` returns the authored object unchanged while accepting optional `setup` and `teardown`.
    - Test 2: The exported run context types use Playwright `Page`/`Locator` and expose direct `chapter`, `step`, `narrate`, `waitForStable`, `highlight`, `snapshot`, and `assertVisible` helpers.
    - Test 3: Lifecycle typing allows only top-level `setup` and `teardown`; no chapter-level or step-level hook types are introduced.
  </behavior>
  <action>Create `packages/sdk/src/runtime-types.ts` and move the public context types there. Import `type Page, Locator` from `playwright` and define concrete option bags with minimal Phase 2 scope: `ChapterOptions` only for future-proof metadata like `id?`; `NarrateOptions` for `voice?`, `instructions?`, and `cacheKeyHint?`; `WaitForStableOptions` for `state?: "load" | "domcontentloaded" | "networkidle"` and `timeoutMs?`; `HighlightOptions` for `name?` and `paddingPx?`; `SnapshotOptions` for `name?`; `AssertVisibleOptions` for `timeoutMs?`. Update `packages/sdk/src/tour.ts` so `DemoHunterTour` stays a plain object model per D-01/D-02 with optional top-level `setup` and `teardown` only per D-08/D-09, `run` receives `DemoHunterRunContext`, and `defineTour<T extends DemoHunterTour>(tour: T): T` remains identity-only. Update `packages/sdk/package.json` to declare `playwright: \">=1.59\"` as a peer dependency so authored tours get real Playwright types without bundling a custom browser abstraction, per D-07.</action>
  <verify>
    <automated>bun test packages/sdk/src/tour.test.ts</automated>
    <automated>bun x tsc -b packages/sdk/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/sdk/src/runtime-types.ts` exports `DemoHunterLifecycleContext`, `DemoHunterRunContext`, and all six Phase 2 option-bag types.
    - `packages/sdk/src/tour.ts` defines `DemoHunterTour` with required `id`, `title`, and `run`, plus optional top-level `setup` and `teardown`.
    - No builder API, fluent API, nested helper object, or chapter/step lifecycle hook type is added.
    - `packages/sdk/package.json` declares a Playwright peer dependency at `>=1.59`.
  </acceptance_criteria>
  <done>The SDK owns a complete Phase 2 authoring contract that downstream runtime work can implement against without further signature discovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export the Phase 2 SDK surface and prove the contract with focused tests</name>
  <files>packages/sdk/src/index.ts, packages/sdk/src/tour.test.ts</files>
  <read_first>
    - packages/sdk/src/index.ts
    - packages/sdk/src/config.test.ts
    - packages/sdk/src/tour.ts
    - packages/sdk/src/runtime-types.ts
    - tests/e2e/workspace-build-contract.test.ts
  </read_first>
  <behavior>
    - Test 1: The SDK entrypoint re-exports `defineTour` and the Phase 2 runtime/tour types from `dist`.
    - Test 2: A representative authored `defineTour({ setup, run, teardown })` object typechecks and preserves reference identity.
    - Test 3: Inline helper usage compiles from the context surface without requiring imported standalone helper functions.
  </behavior>
  <action>Update `packages/sdk/src/index.ts` to re-export the new runtime/tour types from `./runtime-types.js` and `./tour.js`. Create `packages/sdk/src/tour.test.ts` as the focused Phase 2 contract test instead of continuing to overload `config.test.ts`. Assert that `defineTour` preserves object identity, that authored objects can include `setup`, `run`, and `teardown`, and that the runtime helper names are available on the context object rather than as imported functions, explicitly enforcing D-06. Keep the test runtime-only where possible; use compile-time-friendly fixtures in the test file to prove type intent without adding a separate test harness.</action>
  <verify>
    <automated>bun test packages/sdk/src/tour.test.ts</automated>
    <automated>bun x tsc -b packages/sdk/tsconfig.json --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `packages/sdk/src/index.ts` re-exports the new runtime/tour types needed by downstream plans.
    - `packages/sdk/src/tour.test.ts` is the source of truth for the Phase 2 SDK contract and passes.
    - The tests prove context-first helper access and do not import standalone `chapter`, `step`, or `narrate` functions.
  </acceptance_criteria>
  <done>The SDK public surface is stable, explicit, and covered by tests that pin the locked Phase 2 authoring decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored `.tour.ts` -> SDK contract | Untrusted user code is typed against the public authoring surface. |
| published `@demohunter/sdk` -> Playwright | Package consumers rely on the SDK to expose Playwright-native types without replacing Playwright semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | `packages/sdk/src/tour.ts` | mitigate | Keep `defineTour` identity-only so the SDK cannot mutate authored tour objects or hide runtime behavior. |
| T-02-01-02 | E | `packages/sdk/package.json` | mitigate | Declare Playwright as a peer dependency and use Playwright types directly instead of shipping a DemoHunter-owned browser abstraction. |
| T-02-01-03 | S | `packages/sdk/src/runtime-types.ts` | mitigate | Expose only top-level lifecycle hooks per D-08/D-09 so helper signatures do not imply unsupported hook scopes. |
</threat_model>

<verification>
- `bun test packages/sdk/src/tour.test.ts`
- `bun x tsc -b packages/sdk/tsconfig.json --pretty false`
</verification>

<success_criteria>
- Authors can write a `defineTour({ setup, run, teardown })` object with Playwright-native types and direct helper names on context.
- The Phase 2 contract does not introduce a builder DSL, nested helper namespace, or non-top-level lifecycle hooks.
- The SDK public entrypoint exports everything downstream runtime and validation plans need.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tour-authoring-sdk/02-tour-authoring-sdk-01-SUMMARY.md`
</output>
