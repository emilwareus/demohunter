---
status: complete
quick_id: 260611-l8l
slug: add-natural-text-entry-api-for-narrated-
created: 2026-06-11
---

# Quick Task 260611-l8l: Add Natural Text Entry API for Narrated Typing

## Goal

Add a `typeText` helper to `narrateWhile` so visible text entry can be typed incrementally with deterministic natural pacing.

## Tasks

1. Extend the SDK timeline types and exports with `typeText` and `TypeTextOptions`.
2. Implement deterministic Playwright-backed typing in the generator runtime, reusing `narration-sleep` timing and replay accounting.
3. Add runtime/replay tests and update authoring docs/examples to show visible typing via `typeText`.

## Verification

- `bun run typecheck`
- `bun test packages/generator-playwright packages/sdk`
