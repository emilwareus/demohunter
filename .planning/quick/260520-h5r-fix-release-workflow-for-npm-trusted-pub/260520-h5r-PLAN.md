# Quick Task 260520-h5r: Fix release workflow for npm trusted publishing

**Date:** 2026-05-20
**Status:** Complete

## Goal

Update the GitHub Actions release workflow so npm publishes use trusted publishing/OIDC instead of a missing `NPM_TOKEN`, while preserving recovery for the already-created `v0.1.1` tag.

## Tasks

### 1. Update release workflow publishing path

**Files:** `.github/workflows/release.yml`

**Action:**
- Use Node 24 for npm trusted publishing support.
- Disable package manager caching in the release setup.
- Remove `NODE_AUTH_TOKEN` and publish without explicit `--provenance`, relying on npm trusted publishing.
- When npm is missing a version but the matching git tag already exists, check out that tag before dry-run and publish so reruns publish the tagged package.

**Verify:**
- Inspect workflow diff for tokenless publish and correct recovery ordering.
- Run repository verification.

**Done when:**
- Release workflow is ready to publish via trusted publishing and can recover `v0.1.1` with `bump=current`.
