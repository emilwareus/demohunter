---
quick_id: 260611-kc0
status: planned
---

# Quick Task 260611-kc0: Fix TTS Language Review Findings

## Scope

Address review findings from the explicit TTS language implementation.

## Tasks

1. Normalize narration language values so whitespace does not fragment cache keys or leak into provider requests.
2. Add resolver coverage for config-level language fallback when per-call language is omitted.
3. Clarify docs that `language` should be an ISO 639-1 code such as `sv`.
4. Run focused tests, full tests, and re-review the branch.
