---
quick_id: 260611-j3z
status: planned
---

# Quick Task 260611-j3z: Add configurable narration language for OpenAI and ElevenLabs TTS

## Scope

Add optional narration language configuration to DemoHunter without changing provider auth or the local-first OSS boundary.

## Tasks

1. Add `language?: string` to SDK/CLI TTS config and narration override types, then resolve it into narration requests.
2. Map language to provider behavior:
   - ElevenLabs: send `language_code` through provider options as the documented API field.
   - OpenAI: fold language into speech instructions because the current speech API reference does not document a universal language body parameter for built-in voices.
3. Include language in cache identity, update docs/examples, and verify with focused tests.

## Verification

Run focused tests for config loading, narration resolution, cache keys, and both providers.
